matplotlib
tensorboard
seaborn==0.9.0
imageio==2.6.0
scikit-fmm
scikit-image
scikit-learn==0.22.2.post1
ultralytics>=8.0.0
opencv-python>=4.7.0
faiss-cpu>=1.7.2
# NSO 新增依赖
# openai CLIP（开放词汇语义密度场 OV-SDF）
# 安装：pip install git+https://github.com/openai/CLIP.git
ftfy
regex
# GroundingDINO（可选，OV-SDF 最优检测前端）
# 安装：pip install groundingdino-py
# 如未安装将自动降级到 YOLOv8