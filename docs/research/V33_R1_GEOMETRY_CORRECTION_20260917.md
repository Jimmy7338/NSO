# V33 r1：一次有根因依据的实体几何修正

状态：r1 配置已生成，精确并集和导航检查通过；本说明没有执行完整可见性或收益求解。正式 r1 仅允许一次，失败即停止，不再调整尺寸、预算或收益阈值。r0 的失败与全部源文件保留原位。

## 1. 已有失败依据

来源为 `audit_results/v33_direction_information_r0_20260917` 的已封存 `P00_geometry.json`、`P01_geometry.json` 和 `result.json`。r0 实际构建 4 个解析几何模型，未启动 DP。共同安全图、连通、前缀几何配对、镜像参考面积门均通过；全部目标代表点可观测门失败。

每个父布局的两个假设各有 22 个不可见块，共 3.26 m²。将 P01 的全局坐标逆旋转 90° 后，四个模型均对应以下局部区域：

- 18 块、2.94 m²：护板后表面 `v=1.5`、第一肋前表面 `v=1.6`，以及脊柱侧面 `u=±0.6,v=1.55`。它们位于宽 0.1 m 的夹槽中；代表高度为 0.6333333333、1.1、1.5666666667 m。
- 4 块、0.32 m²：护板背面 `v=1.5,z=1.9` 的顶部带。护板高 2 m，而脊柱和肋高 1.8 m。

这些是 r0 的有限姿态、视场和代表点模型所发现的不可观测面，不泛化为任意连续机器人位姿下的不可观测定理。每块原坐标及分组保存在本次生成回执中。

## 2. 唯一修正

两个父布局、两个镜像假设对称应用以下两项物理变化：

1. 公共前护板仍为局部 `u=[-2.75,2.75],v=[1.1,1.5]`，只将最高点 `z=2.0` 降到 `z=1.8`。
2. 第一肋仍保持原左右侧范围和 `z=[0.4,1.8]`，只将前端 `v=1.6` 延伸至 `v=1.5`；后端仍为 1.8 m。这向槽中加入实体材料，使第一肋与公共护板实体相接，精确并集会去掉内部接触面。

P01 在全局坐标中旋转 90°，所以第二项对应第一肋的全局 `x_max=-1.6→-1.5`，不改变全局 y 范围。配置中的第一肋局部范围说明同步更新。

这不是从原参考分母中筛去难面。新版本真实增加槽内材料、移除超高护板材料，并重新计算全部并集外表面。r0 仍使用原真值和原失败结果。原来可见的一些表面也随实体变化改变，因此不能把全部面积下降等同于 r0 的不可见面积。

除 8 处数值几何边界、2 处同步尺寸说明，以及版本/冻结范围文字外，JSON 其余叶节点完全相同。总预算 42、付费前缀 18、余量 24、全部安全整数中心、全部合法转向/前进、其他肋和主体、传感参数、类别映射、收益函数、覆盖约束和求解限制均不变。

## 3. 精确面积与体积变化

以下数值是准确矩形裁剪后的实体并集量，未使用代表点近似剔除接触面。每行同时适用于该父布局的两个镜像假设。

| 父布局 | r0 竖面面积 m² | r1 竖面面积 m² | r0 全外表面积 m² | r1 全外表面积 m² | r0 体积 m³ | r1 体积 m³ |
|---|---:|---:|---:|---:|---:|---:|
| P00 | 40.52 | 34.80 | 58.60 | 52.88 | 11.336 | 11.064 |
| P01 | 44.12 | 38.40 | 69.40 | 63.68 | 14.456 | 14.184 |

每个模型竖面与全部外表面积均减少 5.72 m²：护板减高贡献 −2.36 m²，填槽并集合并贡献净 −3.36 m²。体积变化为 `−5.5×0.4×0.2 + 1.2×0.1×1.4 = −0.272 m³`。所有外竖面继续计入参考分母；护板没有改为不计分背景。

## 4. 本次实际执行的有限检查

只运行一次新生成器及其纯几何断言，exit 0；回执目录为 `audit_results/v33_r1_geometry_correction_20260917`。

- 先后核验 r0 的 14 个冻结源、5 个产物以及源 ZIP 的成员和字节哈希，均一致。
- 四个 r1 精确材料并集均为闭合、有向二流形；面片积分的有向体积与长方体并集体积一致，镜像面积/体积一致。
- 从整个声明矩形重新判定安全整数中心，P00 为 24 格/96 位姿，P01 为 28 格/112 位姿；两假设全动作图相同，与各自 r0 图也完全相同，全图连通。
- 19 个前缀状态（含第 0 帧）与 r0 逐项相同，18 个付费动作安全且回到精确初始位姿。
- 对前缀所有相机位置与全部肋盒角点作连续前护板投影包含检查，降低后的护板仍挡住这些前缀位置到隐藏肋的射线。此项不是完整几何观测签名复算；正式前缀配对、全部面可观测性和信息价值仍由唯一 r1 正式运行判定。

本次未导入 `DirectionGeometryV33` 或有限收益求解器，没有完整可见性表、World、sensor、mapper、TSDF、Q 或新主任务。它不证明实际 C80、三维重建质量、类别效能或完整 ANS 优势。

## 5. 版本与来源

- 新配置：`configs/virtual3d/v33_direction_scene_r1_20260917.json`，SHA256 `55bc23b57de6e13472970eb1b6670a6e6040db74d3b61e155fe84f9fa08cd7c9`。
- 新生成器：`scripts/generate_direction_scene_v33_r1.py`，SHA256 `cfbb1ce7e9f459f16d02304d4f5ebd35a701b0599245175c4f0c5b1bcd014ec6`。
- 原配置 SHA256 `4bdcf84cab68e205e160f25182cf2ce09cc221fea9a5593d70b57cf03c213390`；r0 manifest SHA256 `ba038cc054b980ba900ae4644cd0d809b0154e9dd35d97e866e022763f2abbf7`。
- 已冻结 runner 会纳入本说明与 r1 配置；生成器本身不在旧 runner 自动源集合内，以上哈希和独立生成回执用于额外绑定，不声称旧 runner 已自动归档该脚本。

生成命令为 `PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv-3d/bin/python -B scripts/generate_direction_scene_v33_r1.py --generate`。生成器拒绝覆盖已存在的 r1 配置；不应再次运行来覆盖本次结果。
