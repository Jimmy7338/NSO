# V25 实体隔板场景草稿——未实现、未验证、未冻结

2026-09-15。主任务已要求停止追加场景实验和几何搜索，本文件仅归档停止前已经形成的设计。没有新增 `env/facility_choice_v25.py`，没有执行进程或 session，没有实例化新世界、运动、传感包、TSDF 或 Q 评价。以下参数未经几何安全检查，不是可用于方法比较的冻结实验协议。

## 拟解决的问题

V24.1 的公共前缀和静态候选已经形成证据，但其廉价斜窥与逐步改选仍可能消除提前语义的优势。原 P01 实际前缀覆盖约 76.96%，未达 80%；这个负例保持，不能通过新分母或静态布局推断它已经解决。完整历史初始化修复后的最近 29 个付费动作净新增均为零，所有实际首选与旧版相同。这些已完成结果应先用于短期语义成败判定，而非继续扩量。

设计草稿以工业工位的正面登记和侧后检修通道为任务解释：前部共同路线看到每个任务设施的类别标记、正面与部分外侧面；未知外露附件位于侧后，实体隔板迫使观察者绕行。所有方法共享传感器、几何规则外壳先验、候选和逐步改选能力，类别不直接改变评分。

## 停止前的一版几何想法

以原 V24.1 的两个父布局、两个设施及原实体高背板为起点；原文件、传感参数、物体尺寸、标记和所有旧证据不改。拟加类别无关、左右镜像的实体 L/U 形侧隔板，而不屏蔽射线。记每设施中心横坐标为 cx、正面纵坐标为 f，A 内侧朝 +x，B 内侧朝 -x。

- 内侧长板拟位于距中心 1.2–1.4 米处，从 y=f−0.8 延伸到 f+2.4；高度 2.4 米。前端另有横向回折，连接柜体外缘 0.8 米处与隔板外缘 1.4 米处，纵向占 f−0.2 到 f，以遮住柜体与长板之间的正面斜窥缝。
- 外侧长板同样距中心 1.2–1.4 米，但从 y=f+0.4 延伸到 f+2.4；前端回折占 f+0.4 到 f+0.6，柜体正面附近的一段外侧面仍拟可从公共通道看到。
- 原背板位于 f+3.2，隔板后端与背板间拟留约 0.8 米物理间距，用作侧后入口与检修通道。是否满足机器人膨胀安全距离、是否形成不可达地面、是否遮死任务关键外形，均未计算。

上述坐标只是首版草图。不存在“这一版已堵住全部廉价视点”“共同覆盖已成功”“简单类已完成外形”或“改选已经昂贵”的结论。尤其需要防止窄缝、隔板前端和机器人原地转向再次提供便宜信息。

## 尚未冻结的静态诊断想法

原计划仅做一次 CPU 单线程、最多 600 秒进程，输出不超过 5 MiB；最多允许一次有明确几何反例的修正，所有失败保留。该计划现已停止，不会自动执行。

拟检查实体几何的安全和连通、完整共同前缀所有姿态在两类别布局下的干净深度一致、物理类别标记可见，以及侧后关键外形至少从某个可达视点可见。前缀投影仅为评价侧的理想射线，不是已付传感历史；不生成或保存大图像副本，不计算 Q、不更新地图。

信息与改选成本应开放所有安全格上的合法原地左右转向和相邻前进，不能只用预设路线的有向边。可先用 GT reachable 图及单个干净像素差异给几何方法更乐观的移动与识别能力，计算提前信息成本；真值仅存在于离线诊断，不能进入运行时。若无法在时间内穷举预算相关视点，须报告已查范围或保守界，不能将抽样无差异当作全局不可揭示。未选定预算区间、信息成本门槛或任何有利于语义的权重，也未获得新成本数字。

若未来重新启动，应先用短期、预声明的最小任务判断语义是否存在可兑现的信息价值，再决定是否实现本草稿。继续实施本场景、实际验证公共覆盖、测量观察质量响应与开展策略比较，均不属于本次已经完成的工作。
