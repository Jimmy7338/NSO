# V25 下一步：一次有界的视点控制修正

2026-09-15。此方案在原P00成绩出现后制定，是开发诊断与候选控制修正，不是独立确认。原四条任务、回放、场景、预算和评价均保留。这里不追加场景修正，不修改重建后端，不训练网络。

**2026-09-16优先级调整：** 本镜像方案保留为可选局部收益诊断，新增四任务暂不启动。先按`V25_SEMANTIC_CANDIDATE_BOUNDARY_CORRECTION_20260916.md`修正在线候选产生及基准的信息边界；镜像选项仍是评价侧预置路线，不能验证语义发现补看目标的贡献。静态通过和原负结果均保留。

## 为什么需要这一步

原A/B满足同一最低可见性条件，但相对设施的视角不镜像，完整动作为354/340；B视点中点相对A的镜像偏西1.5m。跨排列使用同一条路线仍是有效的固定选项比较，但若一个位置选项始终更好，类别对边际改善的影响就可能没有跨过选择边界。仅凭该池的零信息上限，不能断言任何视角下类别都没有信息；也不能断言消除视点差异就一定出现正收益。

已有几何可行见证还表明：理想探看后退回，再完成原双视点补看仍可在400/440动作内完成。因此后续必须允许强G主动探看和改选，不能依赖“选错无法挽回”的解释。

## 固定的两套控制

保持原234动作前缀及原锚点。仅将原完整补看后缀（含返航）关于两设施中线镜像：前进不变，左/右互换，并核查每个实际姿态。形成两套家族：

| 家族 | A侧 | B侧 | 每条总动作 |
|---|---|---|---:|
| 原A视角及其镜像 | A_original（已有） | B_mirrorA（新增） | 354 |
| 原B视角及其镜像 | A_mirrorB（新增） | B_original（已有） | 340 |

两个类别排列均保留，最多新增4条P00主任务及4次回放；原4条只引用，不重采。全部两家族均报告，不选涨分的一套删除另一套。若执行，P00累计8条主任务，仍计入前两周36条总额度和12条信息任务额度。

首先只做一次静态预检：实际两排列的逐姿态安全、原地转向、返航朝向、原预算，以及原16像素/2m/双视点间距0.4m/方位跨度15°条件。简单设施只要求访问相同区域，不为不存在的附件虚构得分。任一预检不通过，保留失败，不绕行、不换点、不放宽门槛；不启动该修正矩阵。预检只检查已有构造的合法性，不计算Q。

## 新采集前必须冻结的分析

两套家族各自列全4个结果和每设施的前缀→终点变化，分别报告最佳固定臂、复杂优先、反向类别规则及两选项oracle。主分析还要给出全部四种路线组成的共同目录：最佳固定路线与四选项oracle；避免通过拆分候选集合人为制造类别信息价值。各方法未来都必须拥有同一目录，不能给G隐藏某个好选项。

类别规则不得事后选择有利视角家族。一个可在新采集前冻结的简单规则是“选实际观测到的复杂设施，再在同设施候选中选较短路线”；反向规则选择简单设施，同样按成本选路线。它是待执行的开发规则，不能替换原批的预定规则或已记录成绩。是否采用及完整平局规则须写入新的执行/分析协议，在首条新增任务前封存。

所有结果保留原C/Q/J、终点资格、实际成本、前缀历史和逐设施变化。公共前缀仍先A后B；世界外围、栅格化和噪声也不必完全镜像，不能称所有混淆已消除。旧结果复用不增加独立样本，两个家族不作为两个父布局。

## 停止和晋级边界

这只允许一次候选控制修正。结构、安全、严重实例或回放失败依执行协议停止未开始任务；低Q不删结果。两家族与共同四选项目录收口后，若条件信息上限仍未达到既定P00开发筛查目标（相对最佳固定路线严格>5%），停止P00继续扩量和收益头训练，不再另挑视角、预算、场景或阈值寻找成功。

若上限通过而固定复杂优先不成立，应明确“复杂就多看”的规则失败，不能把oracle改名为已实现策略。仅一个视角家族有利，最多支持视角条件效应；必须结合共同四选项目录核验，不能包装为普遍语义优势。是否晋级P01和可执行G/S，要依据完整结果及剩余额度另行冻结；P01、噪声与自然类别等尚未见的结果不能提前承诺。

上述静态预检已完成（session70303 exit0）：4/4路线case通过，2个世界、12次理想深度查询，0物理动作/包/TSDF/Q。证据见`audit_results/facility_choice_v25r1_mirrored_options_20260915`，合法镜像路线目录已保存。新增主任务尚未开始；新执行/分析协议与整批容量核算仍需完成。保留ANS及四模块约束，目标仍是可归因的语义和整体净收益；这一步本身不满足该目标。
