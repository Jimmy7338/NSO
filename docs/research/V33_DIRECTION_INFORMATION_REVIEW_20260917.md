# V33 r0/r1 有限信息价值：独立保存证据复核

2026-09-17。复核脚本为 `scripts/verify_direction_information_v33.py`，最终输出为 `audit_results/v33_direction_information_review_r1_20260917`。本报告与脚本在最终审计包中封存；有效性以该包 `result.json` 的 `saved_evidence_verified` 终态及库存为准。

结论：r0 的几何失败被保留，r1 的保存表、12条合法动作见证和均值支持两个父布局分别 **11.79798%**、**10.46832%** 的理想类别信息相对价值，均超过预声明的5%解析筛查门。比较对象是**完成强制18步公共前缀后、24步预算内的有限图最优历史策略**，并非只比较两个固定臂。这里没有实际传感、建图或Q，不能报告为实测语义有效或完整ANS已获优势。

## 来源与独立检查的实际范围

- r0：14个冻结源、5项库存产物（另有库存自身），状态 `geometry_gate_failed`；4个解析几何模型、0个DP实例、0个memo状态。
- r1：15个冻结源、7项库存产物（另有库存自身），状态 `complete`；4个解析几何模型、4个求解器实例、累计30,306个memo状态。两父主求解分别7,983、7,809状态，同结构控制分别6,990、7,524状态。
- 两包的完整文件集合、每个产物SHA、源ZIP成员和字节、当前冻结源逐项核对。旧r0源和产物没有被修正覆盖。
- r1生成器未被旧runner自动收进其ZIP；复核包单独保存 `generate_direction_scene_v33_r1.py` 精确字节，并核其SHA与r1已冻结修正说明的绑定。生成器SHA为 `cfbb1ce7e9f459f16d02304d4f5ebd35a701b0599245175c4f0c5b1bcd014ec6`。

复核器只使用Python标准库，未导入 `DirectionGeometryV33`、原求解器、world、mapper或评价器。它从保存的边表、目标块、mask、签名hash和见证重算：完整安全整数图、平移扫掠安全、转向、覆盖分母、可见面积求和、终点C/F/J、缺块、前缀与返航、最早可辨层、识别时机、逐动作剩余预算、策略均值和门值。

该独立复核**没有重新生成可见性表、原始签名载荷或求解DP**。表内来源和算术一致，不等于独立重新证明全部遮挡积分或主场景最优性。有限求解器先前17项抽象测试及60组无缓存历史策略穷举另见 `V33_FINITE_SOLVER_REVIEW_20260917.md`；本次没有重跑这些测试。

首次checker执行在代数控制处错误使用浮点精确相等：P00两条G见证均值为0.651939655172415，DP及控制为0.6519396551724151，相差1.1102230246251565×10⁻¹⁶；前面的容差核对已通过。失败目录 `v33_direction_information_review_20260917` 原样封存，失败时checker完整源码与解释另存 `v33_direction_information_review_checker_failure_20260917`。只把该比较改为复核器已使用的绝对/相对1e−10容差，再运行新目录审查；这是checker实现缺陷，不是主场景或DP失败。主分数、主源、两次正式几何/DP执行均未改变，没有因该问题重新生成任何物理或求解结果。

## r0失败与唯一r1修正

r0每个父布局的每个假设均有22个在保存有限视点表中不可见的竖面块，共3.26 m²。P00有260个目标代表块、24个安全地面中心和96个位姿；P01有276块、28个中心和112个位姿。两父前缀签名全部配对，但全部目标可观测门失败，因此两父都没有启动收益求解。

r1只按预声明缺陷修改公共护板高度2.0→1.8 m，以及第一肋前端1.6→1.5 m以实体填实窄槽；第二父按90°旋转应用同一修改。复核配置全部叶节点：除这8处几何数值、2处同步尺寸说明和版本/冻结范围文字，没有改变其他配置。预算、前缀、导航图、其他肋、传感、类别映射、收益和资源上限保留。

保存表显示竖面分母P00由40.52降至34.80 m²，P01由44.12降至38.40 m²；每个模型减少5.72 m²。不能把这全部解释成删去r0的3.26 m²不可见面：实体并集变化也改变原来可见的表面。r0仍按原参考失败，r1是新实体版本；不做跨版本分数差的算法归因。完整图、地面中心和19个前缀状态逐项不变。r1四个假设均无保存表意义下的不可见目标块。

## 合格有限策略的收益和动作证据

主量为 `J_potential=C_grid×F_surface`；先逐真实假设算乘积，再按公共1/2先验求均值。表中数字均为解析代理。

| 父布局 | G最优期望 | 正确类别条件最优期望 | 绝对差 | 相对差 | 各见证终点C_grid |
|---|---:|---:|---:|---:|---:|
| P00 | 0.6519396552 | 0.7288553640 | 0.0769157088 | 11.79798% | 22/24=0.9166666667 |
| P01 | 0.5908203125 | 0.6526692708 | 0.0618489583 | 10.46832% | 25/28=0.8928571429 |

每父有G、正确类别oracle、交换类别后允许几何纠正三种政策，每种两个真实假设，共12条保存见证。全部后缀24动作，加18动作前缀为42；每条回到精确原位置和朝向，C_grid≥0.8。复核逐步动作、扫掠安全、mask并集和终点，没有以高均值抵消任一失败分支。

G的配对动作必须在真实观测分开之前完全一致：

- P00：G两条见证的前10个后缀动作相同，第10个动作产生首个几何差异，第11个动作开始分支。这是已保存的“观测后改选”证据。两真实假设的G终点J分别为0.7288553640和0.5750239464，其平均就是表中G值。
- P01：第6个后缀动作已揭示身份，但保存的两条G见证仍采用同一整条24动作路线。不能写成两父都实际改道。代码和抽象测试允许在首次揭示后按真实结构重新优化全部合法剩余动作；本父保存的最优见证没有路线分叉。两假设G终点J分别为0.6526692708和0.5289713542。

这不是通过禁止G探看或锁死长选项得到的对照；有限递推允许前进、左右转、及时停、付费识别和识别后任意合法后续。另一方面，18步前缀是强制任务合同，不能宣称这里是从动作0自由规划全部42步的最强G。未来无强制前缀任务需要重新评估这项约束的合理性。

## 控制与因果边界

同结构双假设控制实际分别求解G与已知值，P00均为0.7288553640，P01均为0.6526692708，信息价值为零。免费几何提前揭示与结构无关cue的结果分别由单点后验/不变后验推得，属于代数控制，不是额外独立测量。

交换类别控制仍可以利用真实几何纠正，P00两分支J约0.5750239464，P01约0.5289713542，本次也均合格。它表示误导先验的控制，不是已知标签固定翻转后理性反解的最优策略。正确类别条件值是假设类别身份已知的oracle，不是从实际RGB、自然语义网络或历史训练学得的类别性能。

两父全部安全格、共同地图/构型先验、动作和预算对G与类别oracle相同。签名保存为可见物理点/法向、单线距离和可见地面中心的hash，排除类别、owner、隐藏面积与评价分母；前缀逐帧配对。独立复核只比较这些已存hash，真实像素是否同样不泄漏仍未测试。

## 可以与不可以得出的结论

可以据本批结果说：在声明的理想、确定性、有限动作/视点与强制前缀合同中，提前知道设备构型方向有超过5%的可见外表面联合覆盖代理价值；两父均有完整合格见证。这为下一次真实传感可行性实验提供了具体候选。

不能据此说：已证明连续空间最强G的界、实际C80、三维重建精度、语义网络效能、ANS四模块优势或大规模多设施效果。代表块的面积分母来自实体并集，但每块可见性由中心判定，实际像素、噪声和可选连续位姿都可能改变识别时间及收益。

2/5/10cm精度结果全部为 `null/untested`，不能将同一无误差可见比例复制三次冒充阈值稳健。外轮廓、实测表面、占用完整性仍须分工评价。本次两版本合计8个解析模型、4个DP实例；复核自身0模型/0DP/0传感/0建图/0Q/0新主任务。主配额仍为16/36，`physical_experiment_ready=false`。
