# V24 完整付费前缀覆盖历史的只读就绪检查

2026-09-15。状态：执行前协议；脚本先经只读审查，本文件落盘不启动检查。本轮只验证完整实际历史初始化的接线与候选变化，不执行新任务矩阵。

输入为已完成且经四次不同进程验证的 `audit_results/facility_choice_v24_paid_prefix_20260915` 四条完整 SensorPacket 序列，另读取已封存 `audit_results/facility_runtime_v24_readiness_20260915` 的八条末帧初始化结果作比较，不重新运行旧版本。P00/P01 分别输入 0–234/0–254 共 980 个保存包，每父有两个类别排列。每条分别构建 N/G 状态，共 8 次新初始化；全部成功时共处理 1960 个已保存帧，原 976 个物理前缀动作被复用，不新增物理动作或传感帧。

唯一算法变化为通过九项解析测试的 `FacilityRuntimeV24/facility_components_v24`，显式传入 `external_prefix_coverage_intent=True`。同一个权威 `ObservedRuntimeMapperV10` 更新一包后立即将实际 belief 与对应雷达事件交给同一个 V21 覆盖账本；初包 start，每次付费动作 observe，末包后 flush 短前缀。所有前缀动作统一使用公开共同覆盖意图，包括转弯、返航、零增益及负增益，不能按结果选取动作或从 GT 覆盖率反推速率。未知到已知按固定 ROI 净新增，失知扣减；保持原每 5 动作、最近 6 前缀及 0.5 速率折扣。完整短前缀也保留，不用乐观生命周期均值替换末段零速率。脚本只根据已有事件做算术校核，不创建第二个地图或账本来重放历史。

候选生成仅接受完整已付包、公共 world_config/shape、实际末包返航锚点、两设施归一化及相同诊断预算。两个父总额仍为 387/421，扣费后剩余 153/167；这些只是静态就绪建议，尚非正式完整任务预算。不得把保存的脚本路线、GT 服务区域、真实几何/尺寸、实际质量、未付候选射线或新几何揭示窗口输入排序。无 N 预热或重新采样。C_ROI 与真实可达覆盖率分开，不能据评价差距补偿分母或阈值。

每个状态只调用一次 choose_goal，另调用一次 plan_observed_return 保留 RPN-UQ 实际调用；不调用 next_local_action、不授权动作、不执行候选。复用上一轮独立核验：TSDF 三数组、去类别 surface/quality、belief/visible 与原采集对比；规划前后再核地图帧数、TSDF 摘要、belief/visible/camera_seen/几何证据及付费账本均未改变。只在内存提取网格摘要，不保存新网格或原始包副本；不调用外形清理/补全后端。

逐帧校核动作/帧身份、coverage_event 对应关系、净新增和失知的连续账本、末帧真实 known 数、雷达观测次数、未生成预测掩码的 union_yield 初值、最终短前缀及最近窗口速率。保留 null、0、所有拒绝与异常。N/G 同一记录及同父类别交换都核共同候选池、去类别资产几何、公共 N 分数及完整覆盖事件一致；新旧版本须有相同地图、付费总账、候选、原 N/G 分值和固定 ROI 进度，预算门和实际选择可以变化。任何意外差异作为未就绪保留。

每条输出所有预算放行、正质量项、非 coverage 角色、按两个实际标记 asset_index 的候选及正质量放行，再列实际 selected 的角色/目标/成本和新旧差异。任意预算放行不等于设施补看；短旋转也不能算择一访问，候选安全仅表示当前观测地图可行，后续动作、到达、终点质量一律不可用。九项测试通过或历史接线正确不意味着 G 通过预算门，更不意味着语义收益成立。

入口和出口核验完整原根/案例 inventory、实际包、独立回放、旧就绪产物、旧 helper 源码与归档；核原 145 及扩展 147 明确冻结清单、新 runtime/test 与九测试证据的 SHA。不扫描新增源码来改写旧冻结清单。保存新脚本、协议及引用 helper/runtime/test 的哈希与小源归档，旧证据一律保持。启动时要求至少 64 MiB 空闲，写 JSON/压缩日志前保留 32 MiB；本次输出（含临时文件）硬上限 16 MiB，普通写入另预留 64 KiB 用于失败回执。预计产物约 8–12 MiB，不把这个估计当作放宽硬限制的理由。空间不足或外层错误须失败退出，已产生案例保留，失败回执若也无法在空间边界内保存则明确打印终端。不得覆盖旧根、重复启动或绕过保留空间。

默认软时限 600 秒，可用 --max-seconds 明确设置正整数。SIGINT/SIGTERM/时限信号绕过逐案例异常处理，终止整批，写失败回执并将根 manifest 标 failed，非零退出；Python 信号只能在可中断边界处理，不能保证立即中断底层 C 调用。工具返回 running/session 只是分段等待，不代表超时失败，由唯一运行方继续轮询同一 session。若被 SIGKILL/系统崩溃等无法处理的终止打断，保留 running 根和终端证据，人工检查后记录中断；不得自动重启或在新目录重做剩余初始化。失败恢复仅归档已有回执、核对未改动的输入和完成状态，再由主任务决定新的授权范围，不自行补跑。

默认新输出 `audit_results/facility_runtime_v24_history_readiness_20260915`。运行命令为 `PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 .venv-3d/bin/python -B scripts/preflight_facility_runtime_v24_history.py --run`。脚本没有等待循环、自动调度或物理实验入口。8 次初始化及其只读选择是本次范围；是否进入新预算/语义选择实验须依据完整诊断另定协议。
