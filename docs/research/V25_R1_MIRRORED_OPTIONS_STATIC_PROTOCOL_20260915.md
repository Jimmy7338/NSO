# V25 r1 P00 镜像选项静态预检协议

2026-09-15。一次有界开发控制检查：已经查看部分 P00 结果与视点偏移后声明，属于事后设计修正，不是独立确认。只判断两个预定镜像选项是否满足原静态采集合同，不评价 Q、不采集新主任务、不证明类别增益。

输入仅为冻结 r1 静态目录和原服务成本目录及其 source/protected-source 来源链。原世界、前缀、预算、评价器与四条已执行/运行中任务不修改。执行前后逐 SHA 核输入；新脚本和本文在首次世界构造及射线查询前封存。单次入口不得覆盖已有输出，也不得换目录重试失败。

- 仅 D25-P00，原公共前缀 234 动作保持不变。
- 两本体中心从原 `route_catalog` 读取，中线 x=8.1 m；结合原 0.2 m 分辨率得列变换 c′=80−c。行不变；朝向 0/2 保持、1/3 互换；`forward` 不变，`left/right` 互换。
- `B_mirrorA` 为原 A 从前缀末锚点开始直至返回的完整 suffix 镜像；`A_mirrorB` 同理由原 B 得到。必须验证镜像锚点仍为原位置与朝向，包含全部返航与转向。
- 原 A/B 各自的两目标视点同样镜像，编号及付费首次到达动作保持对应。不得搜索其他视点、调整访问顺序、绕行、截断、补免费转向或增加停留。
- 只构造两种实际类别排列的 r1 世界，各验证两条固定路线，共 4 个 route case。使用独立正向动作解释器逐状态核对完整路径，并核 `_blocked`、`reachable`、地图边界、原前缀、起终点锚点及两目标付费到达；总动作应分别为 354/340，均不超过原 400 上限。这是静态合法性，不是实际到达或安全执行成绩。
- 只复用原成本模块的 `projected` clean-ray helper，对四个固定镜像视点检查附件可见性：目标为复杂设施的对应世界中，分别渲染完整实体及移除单附件实体；有效深度 >0.15 m、≤2 m，且移除后无命中或命中更远的独有首交像素，每附件每视点至少 16。两视点间隔 ≥0.4 m、相对共同本体中心方位差 ≥15°。使用原合同同一不等式和数值容差；不设置新的像素/深度容差。
- 预期恰为 12 次 clean-ray helper 调用；简单对应场景只做同一路线安全/到达核查，不生成不存在的附件、观察信用或分数。理想射线可见性不保证真实带噪数据中的实例分割或外形质量。

新脚本用禁止调用的方法覆盖 `step/sense/scan`，记录世界构造次数、静态射线调用及误调用计数。禁止创建 SensorPacket、融合 TSDF、计算 Q、读取本批物理成绩、求最短路/成本界或扩展 P01。任一合同或完整性门失败立即保留已完成检查、具体失败、当前输入核验与终止回执；不继续其他验证、不修复重跑。程序异常/信号/时限同样保留，不能覆盖失败。若发生不可捕获的进程终止，保留 running 目录供检查，不自动重启。

只用一个 CPU 核，时限 180 秒；输出连同临时文件 ≤256 KiB，序列化写前保留 64 MiB 实际空闲，额外保留 16 KiB 失败回执空间。原源通过来源链引用，新脚本/协议放入小 sources.zip。记录原输入和新增源码 SHA、路线目录与4条静态 case、深度摘要及附件像素计数、最终清单；不保存深度数组、网格或原始包。若无法保留失败回执而维持空间边界，终端说明并非零退出。

只有全门通过才将镜像目录标为“静态可执行设计”；原 A/B 引用保留，并形成两家族 `{A_original,B_mirrorA}` 与 `{A_mirrorB,B_original}`，不能按 Q 只选择其中一族。未来主比较使用原两臂与新两臂组成的共同四选项菜单，各家族比较仅旁报；不得分割候选池制造信息价值。今后若执行新增四条任务，须另行冻结实际采集及分析协议，仍属于开发检验且受原额度约束；本次检查不授权其启动。完整语义方案仍须与允许探看和改选的强 G 比较。

入口：`PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 .venv-3d/bin/python -B scripts/audit_facility_choice_v25r1_mirrored_options.py --run`。唯一输出为 `audit_results/facility_choice_v25r1_mirrored_options_20260915`。
