图：双资产场景的共同几何前缀与类别交换。上方两列固定展示开发父布局 P0 的第一帧原始观测（step 0），分别对应“西侧储物层架／东侧封闭设备柜”和交换后的排列。蓝色与红色为真实渲染并由 RGB 读取的人工资产标记；描边仅标示已观测标记像素。两帧的深度、位姿、内参与非标记 RGB 完全一致，最大深度差为 0 m。左下为全部 150 个实际付费动作（120 次前进、30 次转向）后由观测融合得到的共同地图及真实运动轨迹，圆点为起点、星号为前缀终点；此图不展示真值几何。

在完整前缀后，P0 两种排列的固定可观察背景覆盖分别为 99.22% 与 99.32%，二维覆盖率均为 C=1。这些覆盖统计来自封存记录的事后真值评价，未进入地图或规划输入。因此，本构造只用于背景基本完成后的三维补看机制正控制，不能证明完整覆盖规划或自然语义泛化优势。原 V8 准备版因候选角色不全未通过整体结构门槛，完整失败版本保留；图中来源尚未执行任何未来候选路线，也没有候选收益结果。

选帧规则：固定选择 P0 已保存前缀中最早的 step 0，同一时间步展示两种排列；没有按未来收益或候选表现选择视点。地图只读取同一封存前缀的 step 150 融合结果。图像与PDF合计 268,107 字节，已实际查看 PNG 并调整图例排版，无文字重叠。

证据与复现：

- 图像：`competition_v8_paired_prefix.png`；矢量容器版本：`competition_v8_paired_prefix.pdf`。
- 逐输入、脚本及图像哈希和所选帧依据：`competition_v8_paired_prefix.json`。
- 绘图脚本：`scripts/plot_competition_v8_paired_prefix.py`。
- 原始来源：`eval_results/competition_v8_preparation_20260911/P0/{shelf_west,shelf_east}/prefix/frames/0000.npz`、同目录前缀动作记录与 `prefix_map.npz`。
- 后验覆盖统计：同一准备目录的 `structure_summary.json`。脚本核对读取文件与原始资产清单和评价前封存一致，不构造世界、不生成新轨迹、不读取候选收益或隐藏几何文件。
