# V27 自主分析嵌套库存恢复补充（2026-09-16）

冻结的只读分析器在创建输出目录前核验采集根库存时停止。采集根 `artifact_hashes.json` 正确包含四个 case 各自的 `artifact_hashes.json`，但分析器用 `path.name != 'artifact_hashes.json'` 过滤当前文件集合，错误排除了所有嵌套 case 库存，导致“库存 3247、实际 3243”的假不一致。没有采集文件缺失或新增；集合差恰好是四个 case 库存文件。

允许一次输出侧恢复：保持冻结分析器、协议、采集来源、指标、配对和判断门不变，只把库存遍历改为排除当前被核验目录自己的 `folder/'artifact_hashes.json'`。恢复包装必须先在合成的根／子目录嵌套库存上证明旧函数失败、新函数同时通过根与子库存，再调用原冻结分析器的 `execute`。原始失败不产生分析结果，不删除或覆盖任何采集证据。
