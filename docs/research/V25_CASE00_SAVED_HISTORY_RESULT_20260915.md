# V25 保存历史验证：拟合改善，输出质量未改善

2026-09-15。唯一执行session12176已正常结束，prepare70434已结束；父进程268048、worker268069，计算60.184秒。执行前冻结150个明确源路径，固定V25参数未改。证据目录为 `audit_results/facility_choice_v25_case00_shape_20260915`，协议为 [V25 case00保存历史协议](V25_CASE00_SAVED_HISTORY_PROTOCOL_20260915.md)。

结论：**近远混合点导致的主要边界外扩明显减少，但两个外壳仍被原一致性门拒绝，最终外形Q没有提高。** 因此这次不能作为共同补全后端验收成功，更不能作为语义或整体方案优势。

|case00实际观测实例|旧V24自由射线冲突|固定V25自由射线冲突|结果|
|---|---:|---:|---|
|slot0，复杂A|5135/8752，58.6723%|68/7960，0.85427%|仍超过0.2%门，拒绝推断|
|slot1，简单B|5224/8950，58.3687%|20/8146，0.24552%|仍超过0.2%门，拒绝推断|

拟合方向误差从约2.42°/2.52°降至0.00499°/0.00852°，拟合深度约0.81050/0.81051m，真实本体深度0.8m。原约12cm正面外扩已明显减少。射线分母随拟合盒变化，因此表中比例是各盒实际被击中的有效射线比例，不是固定样本集上的分类准确率。接近某一门槛不等于通过，也不以此调大阈值。

主外形Q：raw仍为0.4441607156；measured和completed均为0.4652181282；inferred为空、Q为0。新旧四个表示的主Q及J差值全部为0。原二维覆盖94.5122%、零碰撞及返回锚点均来自旧234动作脚本前缀，这次没有新的路径或任务成功。

实际工作量为235个已存包、470次observe、2次完整snapshot和4次冻结V23评价调用，新增物理动作、SensorPacket和TSDF融合均为0。两次snapshot都在导入和构造评价真值前完成；首次seed沿用旧实际marker深度像素。ground、实测/地面/组件/未分配点数组及observed网格与旧结果逐SHA相同，原raw TSDF三数组相同。原raw/measured分数只在网格完全一致后引用，新增inferred/completed才重新评价。

两个实例非空且并非同一组件是最低分离检查；实际observed几何与旧独立分离验证完全相同。每个实例在另一个GT评价窗均missing、Q为0，没有跨窗污染。本次没有新推断表面，不能据此证明未来推断的实例分离性质。

根manifest与result均complete，只表示协议完整执行；原V25整套解析门failed与这次外壳拒绝均保持。9个产物清单已核验，结果SHA为 `034e2921dcbed3761132c9bd75b8d2fed2b4fa4aeeb0024127c4997e9e7f612b`。开发历史已被检查过，不能称独立确认或类别交换验证。

本轮停止该估计器的局部调参和案例扩量，V25不接默认规划。下一场景的静态安全、可观测性与信息代价仍可独立验证；实际语义任务开始前必须明确采用的共同后端和其能力边界，不能因静态通过便宣称本次输出质量问题已解决。按[毕业计划](GRADUATION_RECOVERY_PLAN_20260915.md)尽快完成机制去留判断。
