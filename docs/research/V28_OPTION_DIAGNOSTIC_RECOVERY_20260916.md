# V28 选项级反馈诊断恢复说明（2026-09-16）

首次只读运行在所有终点网格哈希均已匹配后，因校验程序用 `coverage=1` 重算 `joint_outline`，却与 V27 使用实际覆盖率的 `joint_outline` 比较而失败。相同网格的 `outline_macro_quality` 和 `outline_macro_f1` 与 V27 完全一致；差异只来自 `joint_outline = coverage * Q` 的覆盖率参数。失败目录保留，不采用任何中间统计。

恢复版在看到任何有效统计前只做以下修正：

1. 终点一致性评价使用封存的实际覆盖率，继续同时校验 Q、F1、joint 与全部网格哈希；
2. 每个中间检查点只评价主要标签所需的 cue 对应实例，不再重复计算不会进入代理判定门槛的联合宏观 Q；
3. 单实例评价器只包含固定种子映射对应的一个既有参考设施，计算公式、5 cm 阈值、参考网格和外形标签均不变；
4. 输出改用新目录 `audit_results/v28_option_feedback_diagnostic_r1_20260916`，首次失败证据不覆盖。

这是一项执行成本与一致性校验修复，不改变样本、主要标签、三个代理、统计方法或预设门槛。
