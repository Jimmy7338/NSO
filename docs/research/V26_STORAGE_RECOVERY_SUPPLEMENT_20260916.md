# V26 容量故障与原参数恢复补充（2026-09-16）

本补充记录运行恢复，不修改已冻结实验协议、算法、场景、指标、源文件或原始采集结果。

case_01 首次独立回放（session 49779，PID 364228）已执行到动作 400，但写入 `verification.json` 时实际剩余空间低于 64 MiB 门槛；异常处理器写失败凭据也被同一门槛拒绝，进程 exit 1。原 manifest 因而残留 `running`。终端抄录支持执行到最终比较之后，**不替代正式独立回放通过凭据**。这次失败及其 400 个回放动作必须保留并单独计费。

恢复证据目录：`audit_results/v26_capacity_recovery_20260916`。其中保留工具终端抄录、结构化失败记录、修改前 manifest、容量清理记录和恢复收据。关闭且可再生成的两份 APT 二进制缓存共释放 114,462,720 字节分配空间；没有删除实验证据。恢复后继续保持原 64 MiB 门槛。

在确认旧进程已退出、排他锁可取得、170 个冻结源文件、4 项输入、源归档和 case_00/01 全部产物清单仍一致后，只将 case_01 的过期回放状态恢复为 `unstarted`，并在 manifest 添加不可省略的失败历史及恢复证据引用。原物理主任务状态不变，累计主任务仍为 6/36。

只允许一次相同源码、参数、输入的完整 case_01 新进程回放重试，不从中间动作续跑、不按保存指令驱动策略、不把失败改记成功。重试通过后，继续原定 case_02 主任务及独立回放、case_03 主任务及独立回放。任何新执行或证据错误再次停止并保留记录。该操作不增加主任务名额；额外回放执行单独列出。

最终冻结采集器的 `independent_replay_paid_actions` 只统计四份成功回放凭据对应的动作数。汇总报告须另列首次失败的 400 动作及一次重试，给出包含失败的实际回放总成本，不能写成仅运行四次回放。不得改写原采集器汇总来掩盖范围差异。
