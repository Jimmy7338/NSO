# V24.1 共同前缀：静态构型与实际传感配对结果

日期：2026-09-15。**四条实际公共前缀及四次独立进程回放完成，前缀传感可行性门通过。** 两个父布局的两种类别/附件排列在整个前缀中具有相同非语义输入和共同 mapper 几何。四条均返航、零碰撞、观察到两个有深度支持的二值 RGB 标记轨迹。P00 终点覆盖94.51%，P01 为76.96%；本批未比较补看选择、外形质量Q或语义收益。

唯一会话60431正常结束，根 manifest 为 complete，无 failure receipt；没有删除失败轨迹或重复启动。

## 静态构型

新增 [V24.1](../../env/facility_choice_v24_1.py) 继承冻结的 v3，仅把四片工位 L 形背板由2.4 m提高到4.8 m，表示高厂房工位分区。footprint、柜体、外露附件、通道、服务区、0.2 m/90°动作、5 m范围、噪声和0.04/0.12 m TSDF不变。修订在任何V24实际前缀或策略质量采集之前完成；v3及更早的不对称/泄漏静态trial全部保留。

[高背板静态证据](../../audit_results/facility_choice_v24_1_high_backstops_20260915/result.json) 核对四世界的安全地面、前缀及完整动作图最短成本和达到见证，均与v3相同。两父235/255个前缀位姿的全部干净相机像素仍在两排列间相同。每工位正面/外侧中央可见顶缘上方29/18个像素，在矮板版本无有效背景，高板全部得到真实背景，命中高度约3.6594/4.3500 m。这不是所有边缘已有充分外形约束的证明，也没有把0深度解释成自由空间。

[既有布局图](../../audit_results/facility_choice_v24_figures_20260915/layouts.png) 显示相同footprint和脚本路线；本版仅背板高度变化，俯视图不显示高度差。图中原有 proposed 标注保留，本次实际每步姿态已与固定前缀核对。

## 实际结果与独立回放

执行 [冻结协议](V24_PAID_COMMON_PREFIX_PROTOCOL_20260915.md) 和 [配置](../../configs/virtual3d/facility_choice_v24_prefix.json)，逐步调用 world.step 保存完整 SensorPacket。QualityMapperV2 只接收 config/shape 和当前 frame/scan，不用隐藏对象ID、GT服务区或评价窗口建图。

| 父布局 | 每排列付费动作/包 | 终点已知/可达单元 | C2D | 返航/碰撞 | 两标记不同付费位姿数 |
|---|---:|---:|---:|---|---:|
| D24-P00，16×10 m | 234/235 | 2790/2952 | 0.9451219512 | 原锚点朝南，0 | 20/20 |
| D24-P01，18×12 m | 254/255 | 3233/4201 | 0.7695786717 | 原锚点朝南，0 | 18/19 |

同父两排列的表中结果相同。P00 两轨迹累计有效标记深度像素2191/2192，P01 为2157/2173。关联只用实际两种RGB颜色并集、连通分量和深度三维中位数，不读类别或GT身份。公共顺序固定先A后B，A/B自身支持量不必完全相同；本门验证的是两种排列具有相同前缀记录。标记支持不等于完整实例边界、尺寸或外形已经可靠恢复。

共976主付费动作、980保存包；四个不同进程从初始世界逐步重生成传感，再只融合对应保存包，合计976验证动作，无重复raw。主/回放PID分别为251613/251744、251799/251854、251862/251936、252008/252166。全部 fresh packet SHA 与原包精确一致，保存 final_mesh.npz 三数组直接读验哈希，各例回放 vertices/triangles/vertex_colors 均相同。

两父的完整前缀均无非语义首差：动作、位姿、深度、相机/扫描标定与时间戳、扫描距离、二值marker mask、去类别颜色RGB全部配对；每步belief/visible、终点TSDF顶点/三角形及去类别表面/质量统计相同。原类别颜色/标签允许交换，TSDF颜色不作跨排列相等要求；各例独立回放仍核原颜色。GT可达单元的位置随附件排列改变，部分中间C可不同；C只在评价端计算，不能据此误判mapper输入不匹配。

## 已过门与剩余边界

prefix_sensor_feasibility_passed=true；all_prefix_coverage_at_least_80=false。**P01的76.96%只是公共前缀终点，不能称为完整任务达到80%，也不能证明补看、返航后必然失败。** 本批没有计算Q、J、语义选臂回报或自主规划表现，没有要求简单柜达到全部5厘米条件。

静态条件成本：P00仅补看A/B并返航108/108动作，两者198；P01为118/118、两者216。候选余预算153/167、总387/421是执行前的中点建议，尚未经过在线候选可行性验证。精确下界只适用于声明的后方+内侧区域、规定朝向、固定前缀已付费及原锚点返航，不是任意Q阈值的普遍最低成本；换基本路线或其他视区不受其直接保证。GT服务区/最短见证不得生成在线候选。

两父是人工构造的两个环境，两排列是条件配对；四次不同PID确定性回放也不是新环境或独立噪声样本。一个噪声种子、人工彩色标记、精确离散位姿和通用视差参数不能支持自然语义网络、开放词汇、真实ZED/SLAM漂移、机器人自主闭环或四模块整体优势的主张。下一步只读已有前缀检查实际候选、表示和有效边界支持，再决定是否冻结选择实验。

## 证据与存储

[封存结果](../../audit_results/facility_choice_v24_paid_prefix_20260915/result.json)：145份源码/配置/协议，终结后独立核对1003产物SHA及145源SHA全部通过。raw共19.6644 MiB；终结核验时可用51.7461 MiB，未触及50 MiB raw上限或32 MiB保留线。没有清理旧原始或唯一证据。

- result：`270276b95c931a207e201df5414c7d79e652568c0be19f983c99744b2451c8cf`
- manifest：`49be1ba794f519371bc271057937e9f4abfdf8d410b6bb26260a4a7c62d1ed8d`
- 根inventory：`1b5a76b2a1f48158aa58d2059987953cbfc9a0ff84f912d38b5ef0e29bace734`
- sources.zip：`8680dea9f6381406298924e6d83b86fe6bdde4ee8cbb64b9aaf71f1a486b675a`

报告副本、小汇总和核验脚本位于 [新分析目录](../../audit_results/facility_choice_v24_prefix_analysis_20260915/result.json)，未改封存collector、证据或协议。
