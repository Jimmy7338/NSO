# V34 固定路线测量：独立保存证据复核

2026-09-18。**独立复核通过；本批固定路线测量门通过。** 完整四个声明格由 3 条唯一实测轨迹组成，h0 的 G/oracle 为预声明同路线 alias，不能算两个独立样本。P01、在线 G/S、自然语义网络及完整 ANS 未在本批验证。

权威回执：`audit_results/v34_fixed_measurement_review_20260918/result.json`，`status=passed`、`saved_evidence_verified=true`、`full_matrix_collected=true`、`measured_fixed_route_gate_passed=true`。它绑定主批 result SHA256 `a95cb49901cd71523fde07241648268cd9fca30797781d78b80b6130d4d75e16` 和 final seal SHA256 `4fead354291efad4236ddc8ef8ec217971aa4b763651bf53aafe7caf3d7dd28e`。复核结果 SHA256 `4a9606b6b349535ab4451c03ed59f5b79c9be4e8ebfcba571bb5d9c9e04b66dd`；复核库存 SHA256 `25cc24cb2cd2642b9d8c3c483381f3be4bd19a273818f02d3f933d712d5393f3`。

## 结果算术

所有唯一轨迹都实际支付 42 动作并精确返航，碰撞和观测 footprint conflict 均为 0；终点 `C_map=0.9574829932`，通过 0.80 门。初始公共前缀后的 C_map 为 0.7125850340，不作为终点资格失败处理。

| P00 构型/固定路线 | 终点 P@5cm | R@5cm | F1@5cm | C_map×F1@5cm |
|---|---:|---:|---:|---:|
| h0 G/oracle，共用实测 | 0.874542 | 0.748444 | 0.806594 | 0.772300 |
| h1 G | 0.862396 | 0.596466 | 0.705193 | 0.675211 |
| h1 oracle | 0.878204 | 0.742096 | 0.804433 | 0.770231 |

按两个构型均匀先验，G 的 J5 均值 **0.7237555311**，oracle 均值 **0.7712658288**，绝对差 **0.0475102976**，相对差 **+6.5644124%**。2 cm 和 10 cm 相对联合差分别 **+6.2484709% / +6.9723416%**，没有邻近阈值反向。h0 差为 0，全部增量来自 h1；不能隐去这个分解。

这里的 P 是公开 ROI 内预测面准确比例，R 是固定全部真实外竖面的完整度。h1 的差主要表现为 recall 增加，并伴较小 precision 增加。三条终点分别有 **16.3585 / 17.2344 / 16.3747 m²** 原始预测面被公共 ROI 裁去；另去除近地水平面 **0.8330 / 0.8094 / 0.9133 m²**。ROI 外面积已在账本保留，**没有进入 precision 惩罚**，不能把此指标称为全地图或无裁剪完整实例精度。

## 独立核查范围

核查器 `scripts/verify_fixed_measurement_v34.py`（SHA256 `6f678ca0a647f788626e08364f40d16eb0b902ae846fa53966a2c179be904371`）首次唯一执行通过，实际 session9660 exit 0。只读取已封数据，不导入 World/mapper/评价器、不重建、不再次评分。

- source ZIP、prepare 原始键清单、前置证据哈希、三个 main seal、三个 replay seal、最终完整 seal 均通过；公共 ROI 确认来自两构型联合包络，路径和 alias 确认来自预先冻结的像素见证。
- 129 个主 packet 全部字段的身份哈希、实际动作/pose/time/标定、零 semantic 数组、非类别 trace 哈希与 RGB cue 重新核对。三条各 43 包；每次独立回放与对应主结果绑定、PID 配对不同，所有数据相等回执有效。
- 两阶段预测 freeze 清单完整包含原始 packet、raw/extracted mesh、地图、裁取账本和 trace，排除后续评价产物。原始/保留几何的面积与规范化哈希、裁取面积账本、同 belief 上完整 raster 和声明整数中心的覆盖比例、42 预算/返航/资格均复算。
- 四格 alias 映射、跨构型公共前缀同像、全部 2/5/10 cm 的 P/R→F1→J、prefix→final 差与均值/资格门均复算。预测顺序由保存清单与冻结采集代码的执行顺序支撑，没有独立可信时间戳证明。

采集实际计数为 **3 主+3 成功回放**：各 129 包、126 付费动作、129 次融合，共 258 包/252 动作/258 次融合。新增主任务 3，累计 **19/36**。case00 曾因隔离 exec 使用相同数字 PID 而在启动守卫处被拒绝；`replay_launch_rejected_00.json` 明确 0 World/packet/main/replay，随后另一个子进程通过原守卫执行。该记录不是失败的已执行回放，不另增一次传感/融合；数字 PID 在不同命名空间复用也不等于复用了同一进程状态。

本次核查自身 **0 World/传感/融合/表面评分/新主任务**。表面距离及 P/R 没有重新评分，由已通过解析夹具的冻结评价器和独立回放相等回执支撑；clean-depth 仅存 hash，未重新渲染；GT 地面 mask 用于重算覆盖分数，但没有再次构造几何来验证 mask。公共前缀预测相同而不同 GT 构型的 recall 略有不同，不应作为传感配对失败。

本结果支持“这个受控场景中，提前知道设备构型所选的固定观察方向，能在同动作预算下提高实际像素—TSDF 的任务化表面质量”。它还没有证明可实现的语义预测器、强主动几何 G 与自主 ANS 四模块能在线兑现这项增益，也不是实车、自然语义泛化或统计确认结论。
