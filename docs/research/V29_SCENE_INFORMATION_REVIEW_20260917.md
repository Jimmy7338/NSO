# V29 解析场景信息价值：独立只读复核

2026-09-17。结论：未发现阻断当前**有限解析信息模型**结论的实现问题。输入封存、保存路线的可行性、观测签名和面积汇总核验通过。此次未重跑动态规划，故不是独立最优性认证；未构造项目 world/mapper，未生成传感、TSDF、网格、Q 或主任务。

独立工具为 `scripts/verify_scene_information_v29.py`，仅用 Python 标准库重建保存路线经过的有限代表点观测及几何碰撞检查。输出 `audit_results/v29_scene_information_review_20260917/result.json`，状态明确为 `saved_witnesses_and_arithmetic_verified_not_independent_optimum`。命令 `PYTHONDONTWRITEBYTECODE=1 python3 -B scripts/verify_scene_information_v29.py` exit 0，用时约 0.156 秒。

## DP 与 G 的信息边界

`preflight_scene_information_v29.py:420` 在比较前要求两排列的完整动作图相同、公共前缀签名相同、目标代表点可见且安全格连通。G 被赋予共同导航图、两种可能模型和各 1/2 先验，这是协议明确声明的强先验；不是现实中从未知地图冷启动的几何方法。

`uncertain:252–267` 的状态为共同姿态、剩余动作和两个**条件假设**各自的已观测集合。首差前这些集合均由同一动作历史确定，没有实际排列参数进入选动作。动作后的两签名相同就保留等先验；不同则立即分支到两个 singleton 最优后续，按 1/2 合成动作期望。`witness:295–314` 只有实际执行该动作、签名已可区分后，才使用真实排列选后续。物理签名仅为可见坐标与法向，包含背景墙，不含类别、角色、面积或分母（153–167 行）。条件模型里存储隐藏表面积不等于把真实条件分母提供给 G；它们用于已声明模型下的期望规划。

每个递归转移消耗 1 个动作，搜索所有合法前进/转向，只有回到精确锚点才允许取终值和提前停止；返航距离剪枝不排除可行政策。`known:237–249` 的三个分量分别是宏平均、单设施 A、单设施 B 的独立最优值，不是一个政策同时实现三个最优值；主比较只取分量 0，单设施见证另按对应分量重建。未看到强迫 G 保持长选项、禁止首差后改选或使用真实类别提前分支的代码。随机政策上界等于确定性 belief-policy 最优值，是固定先验期望下策略混合线性的数学结论；不是随机游走实验成绩。

本次仅静态检查该递归及保留路径，没有重新计算 192,368 个 memo 状态，没有再次求最优或证明最短成本。结果中的“exact”归属于原封存程序的一次求解声明，独立复核只确认其保存见证及算术与代码边界一致。

## 实际核验的见证与数值

六个原始产物加完整文件集合核验通过；三个冻结源的当前文件、ZIP 条目及声明 SHA 完全相同。用配置逐步重建公共前缀 18 动作、六条主政策各 30 个后续动作，以及四条单设施完成路线。全部转向/前进及保存位姿一致；路径线段与膨胀矩形无碰撞，末端恢复 `(0,0,0)`，没有免费移动或忽略回程朝向。

| 政策 | 排列 0 潜在面积宏平均 | 排列 1 潜在面积宏平均 | 均值 | 各条完整费用 |
| --- | ---: | ---: | ---: | ---: |
| G，未知排列的 Bayes 最优见证 | 0.6750000000 | 0.6620370370 | 0.6685185185 | 48 |
| 正确类别 oracle | 0.6750000000 | 0.6750000000 | 0.6750000000 | 48 |
| 反向类别，首差后允许纠正 | 0.6620370370 | 0.6620370370 | 0.6620370370 | 48 |

独立按实际路径可见代表小面并集复算上述逐设施值、宏平均、完成标记及首次签名 SHA，均与保存值在 `1e-12` 容差内一致。G 的两排列前 11 个后续动作完全相同，第 11 个动作观测首次可区分；它不是根据未见真实排列偷偷选不同支路。原程序另报告理论最早首差为后续第 7 步；本次验证了两条 7 步首差路径和端点签名，**未独立穷尽更短路线来认证“最早”**。不能把原程序的最早可取信息时刻与最优见证实际选择的第 11 步混为一谈。

主绝对差 `0.0064814814814816435`，即 **0.648148 个潜在面积百分点**；相对 G 为 **0.969529%**。四条单设施完成见证的后续费用为 30/28/28/30，总费用 48/46/46/48；可行性与完整代表点覆盖已复核，最短性仍来自原 DP。它们不是新执行的机器人轨迹。

## 控制项的证据性质

- `ordinary_both_simple` 在原运行中另建两个相同几何模型并分别求 G 与 known 值，结果相同约 0.675；这是程序内的另一个计算控制。本复核只检查保存值及源代码，没有重求该 DP。
- `free_early_geometric_revelation` 在源 449、453–454 行直接令两个值等于正确类别 oracle 均值。它是“同等知识必有同等最优值”的代数信息干预，**不是独立执行的免费几何观测或测量**。
- `independent_uninformative_class_cue` 在 457–458 行直接将两个理性最优值设为 G。不相关线索不改变后验，代数关系合理，但 `passed=true` 不是新测量证据；其四种盲从提示回报来自已有正确/交换类别见证。
- `swapped_class_cue` 的两条实际见证已经复核，首次物理签名反驳后允许改用真实模型最优后续。不能称其反向类别一直强行走错。

## 仍然重要的限制

当前增益是**正确类别充分提示、策略又最优时的信息上限**，不是现有方向先验、V29 持久选项或 ANS 四模块运行成绩。开发门只要求绝对差大于 `1e-9`，不能把通过此门替换为旧 5% 效果门通过；六条是两个解析假设下的政策见证，不是六个独立环境样本。

奖励是有限垂直面代表点面积，既无顶底面，也无重复观测精度、噪声、像素、遮挡边界细化或重建误差。共同粗扫后简单柜覆盖 0.35，复杂柜约 0.324074；完成复杂柜并留下粗扫简单柜，得 0.675，反之约 0.662037。这里提前类别的价值来自剩余参考表面积任务的差异，不能据此宣称“多看复杂处提高真实精度”已经得到验证。

接触面采用中心采样整块取舍。复杂柜的保存有限积分为 **17.28 m²**；连续长方体外部垂直面并集实际为 **17.12 m²**：本体 16，减去接触 0.96，加附件外露 2.08。差异 0.16 m² 来自本体接触区域被整块中心规则近似为 0.8 而非 0.96。协议已提前声明该近似，故不构成结果后改指标；但当前不到 1% 的信息优势不应自动外推为连续几何、真实传感或另一采样密度下仍稳定。此次没有为追门重采样或调整预算。

后续若进入传感验证，需要独立检查真实信息窗口、可支付观察及模型质量；本复核不授权新采集，也未增加主任务额度。

## 关键 SHA-256

| 内容 | SHA-256 |
| --- | --- |
| 原结果 | `7b85fffab13cf9dc6c5198828a033ae274398aedb1fe4353e7c462a0fae379d5` |
| 原产物清单 | `b7a6a84f547f51c952d7a1497dab23584d1537d6da9b7cb5257fb1f84c70a475` |
| 原解析脚本 | `e823dd2fdff8361a2cc78f0c77b8c7faf857be021b8b6ca2f586a2c9881b6acd` |
| 原配置 | `265fc45f43e0a91d937bc1c7256f8c72daa8474ac1aa1e507d50f4a0ed6c6d47` |
| 原协议 | `17c2c2575ee2fe5424468d77d3f258c68903400285bdd3d9e221684a21418670` |
| 独立复核脚本 | `0f51babdeeef1ec2a61e9f7fa070691964f7e000ad810d9bc12ce38c4057d08b` |
