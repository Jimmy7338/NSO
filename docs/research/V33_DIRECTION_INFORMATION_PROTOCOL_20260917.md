# V33有限方向信息价值：执行前冻结协议

2026-09-17。本轮执行[V33阶段计划](V33_DIRECTION_VALUE_SCENE_PLAN_20260917.md)的零物理筛查，不运行ANS、传感器、mapper、TSDF或外形Q；不改变V30/V31/V32原结果。主任务保持16/36，去留日期2026-09-29不变。

## 唯一场景与观测边界

初版几何按`v33_direction_scene_20260917.json`和`V33_DIRECTION_SCENE_GEOMETRY_20260917.md`：带共同前护板的设备工位、镜像侧向服务肋、两个不同深度/肋间距的父布局，其中第二父旋转90度。每父两个构型具有相同类别先验1/2；类别仅提示type_A/type_B，相对设备服务侧的关系为公开构型模型，不向几何观测传入方向标签。此为假设类别已知的oracle；未测自然语义或实际RGB可读性。

双方使用同一父布局中完整矩形域的全部安全整数格、forward/left/right动作、公共导航及构型先验。每动作付费1，平移1m、转向90度，圆盘半径.2m，扫掠切向允许。两父总42动作、18动作公共前缀、24剩余，必须返回同一位置和朝向。与旧48步任务不同，新预算为先验冻结的独立任务，不能改写旧覆盖失败。

18步前缀是双方强制执行的任务合同，精确求解范围仅为此前缀之后的全部24步历史策略；不声称从任务动作0自由选择全部42步的最优G。未来自主任务须重新检查公共前缀是否必要，不能把强制前缀下的正收益直接外推到无强制前缀系统。

相机高度.9m、范围4m，针孔96×72对应HFOV90度/VFOV73.739795度；解析观测不是实际像素渲染。单线激光高度.25m、范围8m、180等角射线，无噪声。单线范围与相机范围明确不同。地面中心可见性同样用.25m、8m、全向直线遮挡；它是理想覆盖代理，不冒充扫描栅格地图。

Box并集先准确剔除接触/内部面，两假设使用双方箱体端点的共同切平面，避免不同离散划分制造前缀差异。外表面块最大水平/竖直间距均.5m，面积分母是准确并集面积；每小块的可见性仅按中心点判定，不能声称连续可见面积精确积分。几何签名包括所有可见物理坐标/法向（含水平面和背景）、180个单线距离、可见地面中心坐标，不含类别/owner/隐藏面积/参考分母。真实像素、噪声与更细连续动作可能提供不同信息。

## 主代理与资格

`C_grid`分母为所有声明的可达安全地面中心，重复看到不增加。`F_surface`为每个参考设施已见竖直外表面积比例的等权宏均值，所有真实外露竖面均计分，前护板也不删除。原型只有一个设备工位，不能称已验证大规模多设施。

终点必须`C_grid>=.80`且精确返航，主代理为`J_potential=C_grid*F_surface`。未知假设时优化`E[C_h*F_h]`，不是`E[C]*E[F]`，也不是先最大化面积再乘覆盖。每个可能世界都必须合格，不允许一个失败分支被另一个高收益分支平均掩盖。不可行值保持不可行，不能从负无穷平局生成伪见证。

该量不是实测二维C、外形Q、三维精度或实际联合J。纯无误差可见面积模型没有2/5/10cm精度实验，本轮三项均保存为`null/untested`；阶段计划的阈值稳健性要求留到实际测量前后验证，不能用三组相同代理数值冒充通过。

## 几何门先于收益计算

先一次性建立两父×两假设的表，完整保存：安全图、各参考表面面积/小块、可见mask、观测签名hash、地面中心分母、前缀全部姿态、首个可辨层及路径。要求：

1. 每父两假设的完整动作图相同，所有安全格可达；18步前缀安全且精确返航。
2. 前缀全部相机/激光/地面观测签名相同，且每个目标小块存在可达可见视点。
3. 参考面积为真实几何并集；左右构型面积一致，不靠语义权重制造收益。

任一父的几何门失败，则本版两父均不求收益，不消耗物理任务；保存所有不可观测面/首差证据。只允许一次有明确几何/观测合同缺陷依据的修正，必须另建r1配置、修正说明和输出目录，保留r0；不能因收益未过5%改预算、肋面积、侧路长度或阈值。若r1仍不适用或收益不足，停止该家族。

## 精确有限求解与控制

全部合法动作上做有限时域信念动态规划，状态为位置/朝向、剩余动作、两模型各自累计表面与地面mask及是否已辨别。G具有公共完整图、两种结构和均匀先验；初始/前缀若已揭示则按真实信息更新，不假装未知。主场景配对门禁止这样的提前泄漏。行动后第一次签名不同立即得知真实结构，允许重规划和改选；离开揭示视点后仍记住信息。

正确类别oracle从初始知道构型，其他条件不变。每父分别比较其最优期望与最优G；存在可行且G值>0、完整求解和控制通过时才计算相对信息价值。两个父布局均须严格超过沿用的5%**解析开发筛查幅度**，不能说已通过原实际主指标5%目标。

保存G、正确类别、交换类别并允许几何纠正的两假设动作见证。交换控制可能在实际世界不满足覆盖，必须显式标记，不能进入主合格证据。报告first-information层、政策首差时间、返回、逐世界C/F/J和平均量。随机政策是确定历史政策的混合，因此在此有限模型和固定先验中期望不超过最优G；这不是对连续动作真实系统的全局界。

控制包括同结构双假设（独立解G/已知值应相等）、免费几何提前揭示（单点信念，代数等同oracle）、与结构独立且已知不相关的cue（理性忽略，代数等同G）、交换cue后实际几何纠正。不能把代数干预写成额外独立物理实验。

## 资源与停止

新求解器独立抽象合同已有17项通过及60组无缓存历史政策穷举核对；首次测试夹具预期失败及修订记录另封存，不是场景收益试算。

每版本单进程单线程，总墙钟上限300秒、所有求解器累计1,500,000个memo状态。触限即删失，部分值不是已证最优；不扩大限额。各父表和已完成见证及时写入，小包上限2MiB、64MiB磁盘余量，源码/配置/协议先ZIP封存。输出目录存在即拒绝覆盖或隐式重跑。每版都报告解析几何模型/求解计数与0物理调用，r0失败不得重置主配额。

即使有限信息门通过，实测C80、2/5/10cm重建质量、真实类别线索与ANS四模块兑现仍未验证，`physical_experiment_ready=false`。下一步须冻结实际表面/轮廓/占用评价分工及传感可行性检查，不能立即宣布语义或整体方案成功。
