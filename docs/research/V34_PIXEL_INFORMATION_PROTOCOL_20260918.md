# V34真实像素信息门：执行前协议

2026-09-18。执行[V34阶段计划](V34_MEASUREMENT_GATE_PLAN_20260917.md)的第一道传感准入门。输入是冻结V33 r1配置、完整保存可见性/面积/地面mask及图；不改任何旧源、不重新设计场景、不跑新的主任务。下述“真实像素”指程序化相机像素和单线渲染，不是实车或照片。

## 唯一正式批

两父×两假设：P00每假设96姿态、P01每假设112姿态，共416。每个姿态仅查询一次clean RGB-D与单线，4个World实例、416个packet/clean depth/scan查询；不执行付费episode、不更新mapper/TSDF、不评价Q。新world单元测试可能有额外少量查询，必须另计，不能宣称整个开发只有416次传感。正式输出目录存在即拒绝覆盖或隐式重跑。

观测姿态来自旧表，全四朝向均保留。每帧保存原depth、RGB、相机内参与pose、ranges、laser pose/校准、cell/heading；每个世界聚合一个压缩NPZ，并保存逐字段及全几何SHA，允许不调用渲染器的独立复核。查询中的step=node只用于确定归档身份；这些不是已付轨迹，时间或hyp/episode身份不进入识别签名。

相机96×72、fx=fy=48、高.9米、Euclidean量程4米，沿用现像素渲染近裁剪.15米；无深度/位姿噪声。相机输出axial depth，并显式进行Euclidean4米截断，TSDF的axial depth_trunc将另用于后续融合。单线高.25米、180束360度、独立8米量程。新增packet validator保留身份、rigid pose、RGB-D/scan形状和时序校验，独立检查两量程；旧V10字节不变。

公共地图域为该父全nav_cells的min/max各外扩.5米，.2米栅格；平移实体、地面、相机、雷达、seed及参考为同一mapper原点，原整数中心对齐栅格中心。完整实体投影/半径.2米/切向EPS与V33一致。静态参考报告全栅格可达分母及其两假设hash配对；**没有实际belief，C_map保持null**。原24/28中心覆盖代理不能冒充新栅格覆盖。

## 类别与几何信息

公共前护板的设备局部标记固定u∈[-.35,.35]、v=1.1、z∈[.55,1.25]米；P01完整旋转后判定。只有首交点命中标记才着色，A/B复用现公开两颜色，decoder仅读实际RGB像素。编码的类型不是方向向量；侧向关系仍是公共制造构型先验。它是受控人工标记，未验证自然语义或识别泛化。

G的签名包括全部原始depth、scan/校准/pose及非类别RGB：仅将decoder认出的两标记色统一为相同中性色，保留标记位置、面积和全部其它像素。不能以GT owner、贴片真值mask或隐蔽面表屏蔽额外几何。不向签名放入hyp/类别、semantic数组、GT参考面积或回报。

每父公共18动作前缀的19帧须逐帧几何签名相同；每假设前缀至少两个不同XY位置有≥16像素的正确类型解码，无有意义的相反类型读数。同位置不同heading或重复起终帧不算两独立位置。若失败，保留全部416表，两父均不做收益求解；不增大marker或修改几何以通过本批。

## 一次像素分辨率下的有限复核

用完整像素签名替换V33的代表点几何签名，其余动作图、两假设均匀先验、18强制前缀＋24后缀、精确返航、全部原始reward mask和面积分母均保持。任一像素首差立即为G揭示身份，可以免费利用这次已付观测的所有信息再规划。像素比V33更早或更晚识别均完整报告，不能屏蔽新线索来维持旧收益。

目标仍是V33的理想E[C_grid×F_surface]、每真实分支C_grid≥.8；没有实际表面重建。正确类别known最优应与V33一致，否则行动/奖励合同发生了错误。无类别最优则允许改变。输出两父G/oracle/交换类别后几何纠正的12条见证、最短识别层、见证识别时机、均值和返航/覆盖代理资格。同结构控制另独立求G/known应相等；无关cue与免费几何先揭示为代数控制，不记独立实测。

使用已经抽象穷举核查的同一有限求解器；仅旧保存mask转接，不新构DirectionGeometry或重新算可见性。新数据进主目录前freeze新源码闭包、测试、协议、配置及旧输入hash。全批300秒、总150万memo、2MiB产物和64MiB实际空闲硬限；触限删失，不扩大、不把部分值写成已证最优。

两个父的新信息价值均严格>5%且全部前提/控制通过，才允许推进测量准入。通过也仅说明像素几何信息下的潜在面积收益；2/5/10cm实际精度仍null、actual_C80_verified=false、physical_experiment_ready=false。表面评价夹具和采集冻结须另行通过后才开始最多4主任务，主配额现为16/36。任何门失败就停止本家族固定采集，保留旧V33解析正结果但收窄其解释，不继续调几何或预算。
