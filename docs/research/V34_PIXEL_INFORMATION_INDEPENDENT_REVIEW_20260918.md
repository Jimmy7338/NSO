# V34 像素信息门：独立保存数据复核

2026-09-18。状态 **passed**。新核查器 `scripts/verify_pixel_information_v34.py` 仅使用 stdlib/NumPy，独立重写哈希、RGB 解码、标定、BFS 和见证收益算术；没有导入项目 World、渲染器、mapper、TSDF 或 DP 求解器。首次唯一执行 0.816 秒，exit 0，无失败后重试。

权威回执为 `audit_results/v34_pixel_information_review_20260918/result.json`，其中 `saved_evidence_verified=true`，绑定正式传感预检 result SHA256 `129fb6cb09e0222c78478b5fe925333474d6e47e0cefdb03ca73653296308bde`；复核结果自身 SHA256 为 `00d8546b695b8e84587085fd3c0c6aed7aa725e83e48df2a04e54326c6cb4503`。核查器源码 SHA256 `c2b0da1e07033b7930ab5b7d5362db18970887eaec7a6a4e6e14ebe981215730`；回执目录共 7,878 字节，库存 SHA256 `97788801e7c2936d4db8ca2a52bd688c141760f7046fd1b986b35bf4bbcf1ea8`。

## 复算通过的内容

- 正式 V34 的 11 个封存产物、23 个当前源及源 ZIP；V33 r1 的 7 个产物、15 个源及源 ZIP；正式 manifest 对全部旧输入的哈希绑定，执行前后均一致。
- 四个 NPZ 的全部 416 个节点：原 depth/RGB/intrinsic/camera pose/ranges/laser pose/scan calibration/cell/heading 字段；原始全字段 hash、去类别全字段 hash 和逐字段 hash 均逐一重新计算。帧位置和姿态与已冻 V33 图相符，96×72、欧氏 4 m 相机和 8 m 单线标定通过。
- 类别只从实际 RGB 公开颜色重新解码；并额外验证所有类别像素具有有效 clean depth。四个“父×构型”公共前缀均有 8 个可识别帧、7 个不同 XY 位置，无相反类别读数。19 帧非类别前缀逐帧同像。
- 从完整保存动作图重新 BFS：P00 最短几何首差为前缀后 **8** 动作，P01 为 **6** 动作，与 V33 一致。这里是最短可达信息位置；实际保存 G 见证分别在第 **10/6** 动作首次揭示，不能把两种时机混写。
- 两父共 12 条保存见证逐步核对合法动作、状态、剩余预算、揭示事件、决策来源、18+最多24动作、精确返航，以及从固定前缀与逐观测原 mask 重新累积的终点 mask。面积、覆盖代理、联合值、每政策均值与相对值相符。两个真实假设尚未产生不同观测时，G 的保存动作保持一致。

| 父布局 | G 均值 | 正确类别上界均值 | 相对信息价值 |
|---|---:|---:|---:|
| P00 | 0.6519396551724150 | 0.7288553639846761 | +11.7979798% |
| P01 | 0.5908203125000011 | 0.6526692708333347 | +10.4683196% |

表中均值仍是**旧 V33 理想地面覆盖代理×潜在可见外竖面面积**，这里只把信息分区换成无噪声像素/单线签名。正确类别 known 值与 V33 一致，同结构和代数控制记录相符；本复核没有再次求解最优 DP，因此不声称独立重证最优性。

## 边界与下一步

正式预检实际发生了 4 个 World 构造、416 次 RGB-D 与416 次单线查询；0 paid step、0 mapper/TSDF/Q、0 新主任务。本次独立核查另发生 **0 World、0 渲染、0 DP、0 TSDF/Q、0 新主任务**，不能把这两个阶段的计数混为“整个工作没有传感”。

静态 raster 分母只保存了数量和哈希，没有保存二维 mask。本核查确认两构型的该项哈希一致、来源已冻，但未重新计算 mask 的几何或字节；其正确性依赖新 world 合同测试和已审源码。原像素几何本身也未第二次渲染，哈希复核不是独立物理传感复现。

类别是可见人工 RGB 标记与公开构型映射；没有自然语义网络、未知分布识别或学习收益头验证。全像素首差即完美辨别两假设，仍给 G 乐观信息能力。未测噪声下辨别、C_map、任何 2/5/10 cm 重建质量或完整 ANS 优势。这份通过回执允许继续遵循 V34 已冻结测量准入流程，不能把上述百分比写成实测建图增益。
