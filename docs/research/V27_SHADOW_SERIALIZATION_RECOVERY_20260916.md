# V27 影子验收输出恢复（2026-09-16）

首次影子执行 session 73595／PID 377855 已处理 401 个保存包，终端到达 action400／13 个事件，但保存结果时 NumPy 整数没有转为标准 JSON 标量，结果和异常收据均序列化失败，exit 1。原输出目录封存为当时实际状态；残留的 `running` manifest 不表示进程仍活动，也不构成验收通过。

原脚本 SHA 为 `0430504591e7805dfbcabbd29f491c38d9fafda864f8275bcebeaf054c2463db`。失败证据位于 `audit_results/observed_feedback_v27_shadow_failure_20260916`，原两项产物及其库存、终端、来源和已执行的 401 次离线 mapper／TSDF 更新均保留。

允许一次独立输出恢复：新增包装脚本只将 NumPy 标量／容器规范化为标准 JSON 值，覆盖文件输出及终端异常输出；原反馈代码、输入、13 个事件、256 规划状态、全量支持、公式和门槛不变。规范化必须先用小型数据测试数值与字段一致性，并拒绝非有限值。不得修改原脚本或三份 V27 实现来取得不同结果，不降低 64 MiB 余量、512 KiB 输出或单次 401 包范围。

恢复必须从原 401 包重新完整执行，输出到独立的 `audit_results/observed_feedback_v27_shadow_recovered_20260916`，不能根据失败终端补造正式结果。包装脚本和失败证据一并加入哈希链。若恢复再次失败，保留失败，不自动追加第三次执行。

原失败与一次恢复合计最多 802 次旧包离线融合，另于先前的 401 次全量支持诊断计费；均无新物理主任务或 Q 评价。即使恢复通过，结论仅为固定历史上的反馈支持与写入链通过，完整 V27 runtime 的新自主路径和终点效能仍未验证。
