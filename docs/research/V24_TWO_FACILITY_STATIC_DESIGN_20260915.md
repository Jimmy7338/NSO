# V24 两工位选择世界：静态可行性与信息窗口

日期：2026-09-15。状态：新世界 v3 的静态可行性完成，**尚未执行 V24 付费物理轨迹、TSDF 融合、参考生成或策略质量评价**。这不是语义效果结果，也不表示在线覆盖已通过。

## 文件与接口

- 世界：[env/facility_choice_v24.py](../../env/facility_choice_v24.py)，`FacilityChoiceWorldV24(parent, assignment, sensor_model='iid_025px', noise_seed=1901, semantic_condition='aligned')`。
- 静态审计：[scripts/audit_facility_choice_v24.py](../../scripts/audit_facility_choice_v24.py)。`StaticWorld.step` 明确拒绝任何物理动作；单位成本 BFS 使用离线真值安全动作图。
- 配置：[configs/virtual3d/facility_choice_v24_static.json](../../configs/virtual3d/facility_choice_v24_static.json)。执行命令：`PYTHONDONTWRITEBYTECODE=1 .venv-3d/bin/python -B scripts/audit_facility_choice_v24.py --output <新的目录>`。已有目录拒绝覆盖。
- 正式静态证据：[result.json](../../audit_results/facility_choice_v24_static_backstops_20260915/result.json)、[源码 SHA](../../audit_results/facility_choice_v24_static_backstops_20260915/source_sha256.json)、[产物 SHA](../../audit_results/facility_choice_v24_static_backstops_20260915/artifact_hashes.json)。小源码包仅包含本次新增 world/script/config。
- [两父静态布局图](../../audit_results/facility_choice_v24_figures_20260915/layouts.png) 显示固定的 A 复杂/B 简单排列、实际实体、离线服务区和未执行的前缀，不以扇形冒充可见区域。

继承 V19 的 `sense/scan/step/instance_mesh`、`objects` 评价边界、`config/intrinsic` 及精确离散位姿契约。相机和单线扫描仍为 5 m；96×72 输出、0.2 m 平移、90° 转向、视差噪声主模型 0.25 px，参考 fx=480 px、baseline=0.12 m，输出 fx=48 px；TSDF 配置保持 0.04/0.12 m。这里仍是通用敏感性模型，不是 ZED 实测标定。`semantic_condition` 只改变标签，真实深度不按类别调噪声。

`prefix_spec_v24(parent)` 输出每个正常动作、逐步姿态、阶段索引、付费总数和最终锚点。`service_regions_v24()` 给出声明任务的后方、内侧区域与朝向，**仅用于离线可行性**。`objects`、服务区、完整图和前缀真值路线不得作为运行时的实例关联、候选生成或安全地图输入。后续在线候选仍须从实际观测生成。

## 真实几何与两排列

| 父布局 | 尺寸 | A 正面中心 (x,y) | B 正面中心 (x,y) | 起点/决策锚点 (x,y) |
|---|---:|---:|---:|---:|
| D24-P00 | 16×10 m | (4.6,5.6) | (11.6,5.6) | (8.1,1.3) |
| D24-P01 | 18×12 m | (4.8,5.8) | (13.4,5.8) | (9.1,1.3) |

锚点朝南；中央分隔墙把后部空间分成两支路，南侧保持公共通道。柜体均为 1.6×0.8×1.6 m 封闭外壳。复杂柜额外具有尺寸 0.60×0.64×0.52 m 的后部安装体、0.40×0.40×0.50 m 的上侧凸起；附件与柜体为真实实体连接，使用并集外表面，没有内部架层。B 将 A 的侧后附件和补看侧左右镜像，标记仍在相同 y 正面。两排列为 `A_complex_B_simple`、`A_simple_B_complex`，前标记分别交换类别 3/2。

两工位各有真实 L 形背板：后板位于正面 y+3.2 m，内侧板的近面距中心 x 为 2.9 m，高 2.4 m。公共正面视点到后板的轴向距离为 4.5 m，外侧视点到内板为 4.8 m，提供部分 5 m 量程内背景射线，同时保留后方和内侧补看通道。它们不能保证每个边缘都有背景支持，尤其上缘射线可能越过板顶；没有新增俯视能力、免费视点或无深度即自由空间规则。

## 同一付费基本观察前缀

前缀先给 A、再给 B 正面与外侧的基本记录，最后返回同一锚点，所有移动和转向均计费。先在外侧偏 0.9 m 的 x 坐标向北接近，再于距正面 1.3 m 处横移到中央正面视点；这是物理遮挡路径，不是抹除真实可见结构。外侧视点距柜体中心 x 为 1.9 m，y 为正面+0.1 m，朝向柜体。

| 父布局 | A 正面 | A 外侧 | B 正面 | B 外侧 | 完整前缀/回到锚点 |
|---|---:|---:|---:|---:|---:|
| D24-P00 | 动作 46 | 66 | 162 | 182 | 234 |
| D24-P01 | 动作 51 | 71 | 177 | 197 | 254 |

初始两排列的实际零动作深度、扫描、标定和位姿 SHA 相同，同一步重复深度确定。另对完整提议前缀的 235/255 个位姿调用纯几何相机射线求交：**所有干净像素深度两排列完全相等**，没有保存传感包或将离线深度送入规划器。正面阶段按真实 marker 表面判定各自有 351 个标记像素，另一对象为零；外侧阶段没有正面标记像素。这说明静态的“先看到标记、附件仍遮挡”窗口存在，不保证在线实例聚类能正确保留两个对象。

这些检查不能替代正式付费采集中的完整 raw packet、噪声、RGB 非标记部分、扫描、mapper 状态与候选一致性核验。前缀实际是否生成足够的共同外形支持，也尚未验证。观察 A 比 B 早是共同固定顺序，两排列保持该顺序；后续应保留过程指标，检查时序对称性与关联遗忘。

## 有效动作图成本与预算建议

每个补看任务声明必须访问该对象的**后方及内侧**两组区域，且采用规定朝向：后方 x∈[cx−0.3,cx+0.3]、y∈[front+2.3,front+2.7]、朝南；A 内侧 x∈[cx+1.9,cx+2.3] 朝西，B 内侧 x∈[cx−2.3,cx−1.9] 朝东，y 均为 [front+0.1,front+0.5]。抵达或转向产生其正常付费观察，不插入免费帧。前缀没有访问这些任务位姿，服务位掩码为零。

BFS 穷举全部物理安全 `(row,col,heading,visited-region-mask)`，平移和转向的边成本均为 1；首次回到原锚点及朝向并满足任务位掩码，得到精确最短值。保存达到该下界的动作/姿态见证，并独立逐动作核对可达性、区域访问和返航。四个世界均验证，无免费移动和姿态跳转。

| 父布局 | 仅补看 A 并返航 | 仅补看 B 并返航 | 两对象均补看并返航 | 可择一但不能完成两者的余预算区间 | 建议余预算 / 总预算 |
|---|---:|---:|---:|---:|---:|
| D24-P00 | 108=98 平移+10 转向 | 108=98+10 | 198=180+18 | [108,197] | 153 / 387 |
| D24-P01 | 118=108+10 | 118=108+10 | 216=198+18 | [118,215] | 167 / 421 |

两排列的表中成本相同。建议预算按区间中点向上取整，在任何质量结果之前确定；**尚未冻结为物理实验预算**。该下界只针对已声明区域和朝向任务，不是任意实现某质量阈值的最低成本。它也以共同完整前缀已经付费为条件，不能证明换一条基本路线后仍无法完成两对象，更不能证明公共前缀或最终任务达到 80% 二维覆盖。后续在线已知图可能带来额外路线成本和取消，必须实测，不能用离线最短路线强行保到达。

## 静态修订及未过证据门

首版 [trial](../../audit_results/facility_choice_v24_static_20260915/result.json) 的 A/B 成本不对称：P00 为 108/128，P01 为 118/144。镜像 B 后的 [trial](../../audit_results/facility_choice_v24_static_mirrored_20260915/result.json) 达到对称，但相机正向远距离接近时，附件透视越过正面边缘，P00 的 20–28/124–132、P01 的 25–33/139–147 动作各有 5–10 个像素泄漏。两个 trial 与各自新增源码包完整保留。当前版修正真实进场位置，并加入量程内实体背景，最短服务成本不变，前缀静态深度配对通过。以上均为策略质量评价之前的静态设计修订，不是物理失败后删除或重采。

下一门是一次付费共同前缀检验：完整非语义历史及观测候选配对、两个观察实例关联、共同模型有无有效边界支持、在线安全图及返航可行、实际覆盖分母与 C2D。其后才能冻结 A/B/公共选项、终点预算、共同后续与独立回放协议。若共同记录不足或额外路线无法在线支付，应保留失败并诊断，不能用 GT 服务区、隐藏尺寸、未来质量或者离线见证替代在线决策。

两对象静态世界不能证明大规模优势、语义收益、学习预测有效性或四模块整体优越性。
