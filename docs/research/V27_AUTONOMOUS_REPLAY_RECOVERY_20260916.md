# V27 自主回放容量恢复补充（2026-09-16）

V27 case 0 的第一次独立回放完成了 400 个动作并到达终点比较阶段，但在写 `verification.json` 时因实际空闲空间低于冻结的 64 MiB 保护线而失败；随后失败收据也被同一保护线拒绝。该次回放不算正式验证通过，400 个已执行动作必须计入回放成本。

容量下降的直接外部原因是共享系统中另一个已经结束的工程编译新增约 765 MiB 构建产物。该新构建未删除。空间恢复仅删除 `/root/viscanner.udp/build`：这是 2026-09-08 后未更新、无进程占用、可由保留源码重新生成的旧构建输出。两个受保护 OTA 目录及全部实验包、模型、评价和唯一证据均保留。

允许一次完全相同的 case 0 独立回放。恢复操作只能把根清单中 case 0 的 `replay_status` 从陈旧的 `running` 改回 `unstarted`，并追加一条操作恢复历史；不得修改冻结源码、输入哈希、主任务结果、包、决策日志、网格或指标。重试仍使用原采集器、原来源归档、原 400 动作预算和原 64 MiB 保护线。重试失败时停止批次，不再自动重试。
