# V35 在线四模块自主开发采集协议

2026-09-18，首次V35新World/TSDF/Q前注册。前置33项当前单测通过；[保存观测准入](V35_SAVED_OBSERVATION_GATE_PROTOCOL_20260918.md)8缓存闭环与2去类前缀、2同状态后验干预全部通过，0新物理任务。缓存结果不能代替本批实测。本协议沿用[V34测量合同](V34_FIXED_MEASUREMENT_PROTOCOL_20260918.md)和[表面定义](V34_SURFACE_MEASUREMENT_CONTRACT_20260918.md)，不改变几何或指标。

## 唯一矩阵与资格

使用冻结V33 r1的P00，分别h0/h1，各执行G、S、swapped、swapped_no_feedback，按此前缀次序共8条完整在线采集。G不用RGB类别；S从当前RGB形成类别先验；swapped只交换类别解释；swapped_no_feedback同时关闭实际几何后验和规划中的未来几何纠错。所有组仍更新实际地图与曝光集合，后端一致。缓存同路线不用于alias，8条均真实执行并独立回放，不能当8个独立场景。

累计主任务从19/36开始，创建case目录即计一次，最多新增8至27/36；失败不重置、不自动重跑。每条初始帧＋最多42个付费动作，18动作公共前缀保持原样，之后每动作依据当前实测状态在线选择；禁止读取V33/V34 witness动作串。实际C_map≥80%、42内精确返航和零执行碰撞为硬门。任一资格失败、采集异常或实际独立回放失败，停止剩余新采集并保留全部数据；完整不合格任务仍须尝试其首次独立回放。不能用高J抵消资格失败。

## 共同传感、表示与已知信息边界

沿用V34的96×72相机、4米相机量程、8米单线，iid_025px噪声、seed1901、同父同付费step随机规则、精确模拟位姿。Open3D TSDF voxel=.04米、truncation=.12米、depth_trunc=4米，semantic数组全零；类别不给融合、评价或GT补全加权。不调用学习型重建，不改变地面／ROI提取。

两组同样知道两种公开CAD模板、全部公共安全姿态图，以及两模板的预测深度／雷达与理想曝光，不知道当前实际构型。它是有场地与设施模型先验的主动建档，不能称完全未知地图探索。实际C分母、GT表面和参考评分仅评价器可读，控制器不读`world.pose`或实际hypothesis；位姿由当前packet的相机外参与公开坐标平移、观测heading转换。episode与frame文字身份不输入策略，适配器使用opaque-step标识。

控制器使用已冻结`ObservationV35`边界和四模块：OV-SDF记录实测表示与语义假设；IGCR依据新深度／扫描残差更新后验；STGHP用当前概率与剩余预算计算有限全视界选择；RPN-UQ执行当前局部目标及保守图／返航检查。全局选择是当前长视界的下一姿态子目标，局部执行为1米或90度原语；不称已运行原ANS神经网络或已训练风险模型。

鲁棒似然、类别0.9先验和单次未来观测预测的0.99假定完全继承已冻结准入代码。后者未校准且不是精确POMDP；G和S具有相同主动取信息能力、同一解算器、边顺序、1e-12择优容差、每select最多1,500,000 memo/20秒。已付实际帧先进入后验，才允许产生下一动作；不能在正式运行按成绩改变参数。

## 原始证据与重放

先保存初始及每次动作的原始packet，再核动作、位姿和碰撞；异常时也保留已生成的帧。地图只融合一次。逐步保存动作、四模块调用、posterior、类／几何log-odds、当前地图摘要、global plan与局部合法性、实际计数；不将模板预计覆盖写成实测完成度。

付费18动作及终点保存原始TSDF、公共ROI提取网格、belief数组及裁剪账本。全部预测快照、轨迹和模块trace先封存，再读取评估GT和计算P/R/F1@2/5/10cm及C_map。precision不惩罚公共ROI外几何，裁去面积照常单列。主量仍J5=C_map×F1@5cm，三条阈值的P、R、F1、连续误差同时报告。

每个完整主任务后，在全新Python进程重新构造world、mapper、控制器，以相同当前观测重新作在线决策，核所有原包、控制器/动作轨迹、两阶段地图/网格和评分。不能只回放保存动作或只重算分数。进程独立性用启动时间和进程身份共同记录，兼容隔离命名空间局部PID复用；不得以改PID变量伪造新进程。预启动环境拒绝与真正开始后失败分别记录，实际失败不重跑。

采集／回放每进程600秒、CPU单线程；每case含回放≤40MiB，共享数据≤2MiB，保持实际64MiB空闲。需要时仅采用用户已授权的无损精确重复去重，不能删除唯一证据；本批不预设需要清理。

## 冻结分析与开发通过条件

以两构型等权平均比较所有8格，分别报告S−G、S−swapped、swapped−swapped_no_feedback的每构型差、均值、绝对／相对J5差及2/10cm。类别消融不改变几何、路线能力、建图或评价；不得根据哪个阈值有利来选择主指标。若未收齐8格，均值比较保持不可用。

开发行为与效能门为：全部8条合格、8次首次实际独立回放通过；S−G主均值严格为正且2/10cm不反向；正确类别相对错误类别J5均值严格为正；开启几何纠错相对关闭纠错J5均值严格为正，且至少一构型存在实测几何后验更新后行动改变的记录。若只满足其中一部分，分开报告语义与反馈，不宣称完整开发门通过。

对反馈行为再作预定同状态干预：每构型swapped的首次非零几何更新使真实构型概率跨过0.5时，使用保存的node/masks/remaining/visited及同一未来几何设定，仅替换为错误语义先验重新选择动作，共最多2次额外DP，无新World/Q，确认不是仅因未来信息预期不同而提前分叉。真实构型只用于离线定位该审计状态，绝不回流主控制器。若首状态未改动作，按失败报告，不搜索后续有利状态。

这是单父、两个配对构型、人工颜色与已知双模板的CPU开发证据；不做显著性或独立样本量夸大，不证明自然开放词汇、大规模泛化、原神经ANS、SLAM位姿精度或实车领先。通过后才冻结最多8条噪声留出／跨父物理确认，P01此前已有设计信息不能称完全未见布局。9月29日去留日期不延长，不自动使用第36条名额或重置额度。
