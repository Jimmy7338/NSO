# V36 主任务前独立复核：冻结适配

2026-09-18。静态适配与协议检查通过；本文固定于真实采集之前，不声称实际确认门已经通过。正式批封存后另行记录独立实际证据复核结果。

## 主任务前的源码检查

逐块对比 `scripts/run_online_routes_v36.py` 与已冻结 V35 采集器，允许的变化为批次标识、历史额度 27/36、P00/P01 两父 × 两构型 × G/S 的八格矩阵、350918 固定种子、按父选择公开模板／安全图／可观测 ROI／坐标平移／地图尺寸以及按父配对统计。V35 的 posterior、planner、controller、mapper、metric 源码保持不变。

`env/information_pixel_v36.py` 继承 V34，调用不产生观测的原构造器后仅设置 `noise_seed=350918`。首包生成之前种子已生效。原几何、传感和评价方法均继承原实现；类别和 episode 标识不参与噪声派生。没有把 P01 的坐标、地图尺寸或前缀拿来与 P00 硬比较。

采集器保留原始包先落盘、观察量构造控制器输入、42 步预算、18 步共同前缀、预测与决策先封存再访问评价 GT、新进程重做在线决策回放、失败计入已创建 case 额度及串行停止条件。效能门逐父计算，不能以总体均值为正掩盖某父不通过。

最终采集器 SHA256：`281d73c3d1bc05e89754f1c7386a336d497110067793b6dfee4ace0c42422b11`；种子适配层 SHA256：`2891412680c878f4ba0056d61225987f6f57a824a086cf4533e52fc3937734cd`。首轮完整 diff 检查后仅在源码归档列表加入本文，已核对这一行变动；以上最终字节可随正式批 manifest 冻结。

## 独立审计方法和已完成的实现检查

`scripts/verify_online_confirmation_v36.py` 复用冻结 V35 独立审计中的保存包、后验累计、四模块逐步调用、实际执行动作与新进程回放验证函数。新增逻辑从原始 NPZ 重新计算全部非语义字段哈希、从相机观测恢复姿态、核对各父地图尺寸与 RGB 类别准入，并独立计算每父两构型等权 J2/J5/J10 差和两父等权总体差。

至少一条 S/G 对照须满足：分歧前的非语义观测历史、当前姿态、可见区域掩码相同，RGB 导出的先验及后验不同，在线动作随之不同，并附该配对终点的实际 J5。该审计不重新运行 DP、World、传感器、TSDF 或表面距离评分。P/R 沿用封存的实际评价值，只独立检查保存地图覆盖率和 F1/J 算术；原始几何残差似然正确性继承冻结单元与准入证据。

正式确认前仅用 V35 已有 case00 做只读检查。第一次开发检查暴露 trace 的 V13 header 哈希与 posterior 的 V35 长度前缀哈希编码不同，新审计代码区分二者后，43 份保存包的全部非语义字段、观测姿态、父地图尺寸与类别检查通过。两次检查没有产生新物理观测、DP、TSDF、Q 或主任务；修正只涉及新审计编码，没有修改策略、传感或指标。

独立审计脚本 SHA256：`368baf36dd1a17993a9c78a68c87fac7fbf7c5aa57f8396c9ba75180f436c5cf`。

## 协议与后续独立审计边界

已核对 `V36_ONLINE_CONFIRMATION_PROTOCOL_20260918.md` 与冻结计划：八格顺序、单一种子、父内及父间等权统计、逐父通过门、零额外预跑、失败停止及独立复核均一致。待实际八格采集及回放终止并封存后，运行一次独立审计，结果另写新文件。独立完整门由 `complete_confirmation_gate_passed` 给出；采集器中的待审计字段不在封存后回写。

本次仅检验两父中的正确语义相对主动几何基准。P01 在此前理想几何和全视点检查中已被观察，因此这是冻结策略后的一种未使用噪声实现及跨父实际建图确认，不是完全未见布局的泛化检验。本矩阵不重新验证 IGCR 的错误类别纠错收益，也不验证自然图像语义、原训练 ANS、定位误差或实车。
