# V28 保存历史描述符与候选机制预检（2026-09-17）

本协议在读取新原型的历史快照成绩前固定。输入仅为 V27 的 case_01、case_03 已保存 0–400 包；各取 action=0,50,100,150,200,250,300,350,400，共18个快照。不得按结果加密、删掉或更换快照。重建观测 mapper 一次/轨迹；不提网格、不算 Q、不调用世界或执行动作。新增主任务为0，额度仍12/36。输出限2 MiB，磁盘余量64 MiB，单次总时限900秒，失败保留收据。

## 实现范围

新增 `nso/observed_debt_v28.py`，保留原运行时与冻结证据。描述符取 cue 周围1.5米的全部白名单观测面片；原几何候选的评分仍用原256面片。邻域可能含背景，不能称实例分割。方向支持是面片观测方向位图的均值；这是观测不足代理，不是重建质量或校准不确定性。方向参考为首次观测视线，并非物体轴。帧次数不是独立视点数；有效基线、边界碎片、法向稳定性及校准不确定性当前缺失，必须输出 null。

原 G 的最终至多12候选，加每个可信 cue（>=0.6）的固定1.6米八方向几何环，构成原型候选池；所有路线由同一已观测可行域和含返航预算过滤。类别不参与候选几何生成。环半径、阈值、增益尺度0.05和8方向归一先验均在此次运行固定，不按结果调整。

- G：原 V26 几何方案，零语义输入。
- O：cue位置 + 均匀方向先验，不使用类别；控制物体位置提示收益。
- S：同一候选池 + 类别方向先验；两类总先验质量均为1。
- X：保持观测与位置，交换2/3类别。
- L：实际将置信度降至0.59，重新构建上下文，验证与G完全一致；不能仅用强制回退开关冒充此项。
- S_no_reservation：同一池、同一S分数，关闭实例保留。

短名单至多12条，先保留至多4条原覆盖路线，再对每个有正代理收益的观测cue对称保留一条，剩余按分数补齐。最终动作目标仍取最高分，不强制访问类别3。这不保证 C>=0.8；不具备完整选项生命周期、LCB 或学习残差，不能写成四模块最终系统已完成。

## 事先固定的检查与解释

1. 逐包SHA、累计cue与原审计完全一致；快照几何SHA与原审计一致。封存输入与运行源；结束重核。所有候选含安全返航，成本不超余量。
2. O/S/X 共用同一原始候选池；O在交换类别后完全不变；实际低置信 L 与 G 完全一致；描述符交换类别不变。
3. 分别报告：原始可行正债务候选、短名单、最终选择。实例保留门为“所有 eligible cue 均有候选保留”；若容量不足或得分无正候选，分别报告，不能把不存在候选说成被短名单淘汰。
4. 旧设计20%未写清分母，旧审计又误用已选比例。此次预先采用保守口径：两个cue均出现且原始池存在类别3正债务候选的快照，类别3短名单行数/这些快照全部短名单行数；另列以正语义行数为分母的比例。两排列各>=20%才记该诊断门通过，不能事后替换分母。连续10次缺席门在此稀疏9快照/轨迹不足以完整重现，标记未验证。
5. 报告 S/O、S/X 的排序、短名单和最终选择变化。存在变化只证明类别进入决策，不证明类别正确或有益；无变化也不以隐藏加权制造变化。
6. 这18个快照来自已用于开发的两条固定轨迹，不能形成闭环效果、独立泛化、置信区间或语义性能优势证据。未通过任一开发机制门时保留失败并停止新增主任务，不反复调参追门。

先运行边界/安全/反事实单元测试，再一次运行预检。既有13/24负ΔQ只说明测量表示—清理—指标链条非单调，融合根因仍待隔离；共同建图改动需另立协议。旧179项源、旧诊断脚本及协议不作修改。
