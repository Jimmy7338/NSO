# V24 付费前缀覆盖历史初始化修复

日期：2026-09-15。新增版本，不修改原145个冻结源。原V21在长外部前缀末帧才建立覆盖账本；P01已有254步历史却得到null速率，22个候选预算门全部拒绝。见[V24就绪结果](/root/NSO/docs/research/V24_FOUR_MODULE_READINESS_RESULT_20260915.md)。

新 [nso/facility_runtime_v24.py](/root/NSO/nso/facility_runtime_v24.py) 在同一个权威ObservedRuntimeMapper逐帧更新后，将实际belief及对应扫描交给同一个V21覆盖账本：初帧start，每个已付动作observe，末段flush。无额外地图遍历、GT覆盖分子或伪造预测校准；原动作0路径与正常后续沿用旧实现。原初始化没有逐帧hook，因此新增版本保留复制的ID/时序/动作/预算校验段并标明来源，没有全局替换或monkeypatch。

带付费前缀须显式传入 `external_prefix_coverage_intent=True`，表示公开的外部公共覆盖前缀用途。本版对所有前缀动作统一计费，包括转弯、返航、零及负净known变化，不按实际增益选择片段。V21固定最近窗口与折扣不变；末段低增益仍可能产生0速率并拒绝补看，不能将修复历史接入写成保证预算放行。

9项解析及极小8×8传感包测试通过，正式session80172已exit0。涵盖真实小前缀启动、继承返航授权及下一笔IGCR观测、重复包、类别交换、零/负/短尾记账、末30动作零速率保留、身份/时序/姿态/预算/reset防线及普通动作0兼容。首次测试有3项夹具API错误，修正与原失败计数记录在审计中，没有放宽算法断言。

[测试与证据](/root/NSO/audit_results/facility_runtime_v24_prefix_history_preflight_20260915/result.json)核验原145源前后字节不变。没有加载真实980包前缀、没有新V24.1世界或后续策略试验；实际预算可用性、语义排序和效能仍未验证。本版也未接入共同外形评分。下一次使用真实已保存前缀时须另立协议，保留真实0速率/拒绝，并结合已发现的7/8步几何揭示检查信息窗口。
